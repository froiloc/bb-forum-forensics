{
  "build": 461,
  "version": "0.7.461",
  "date": "2026-07-20",
  "note": "Build 461 (AP-2G Betrieb/Governance, Fremdforum-Promotion, Frontend zu Build 460; mc 2026-07-20; MD5-Handshake bestaetigt). REINES FRONTEND; kein Endpunkt/DB/Migration. Cockpit-Sicht 'promotion' (Gruppe Administration, Recht ops.view) fuer die Fremdforum-Kandidaten (forensic_<uid>.db vorhanden, evidence_<uid>.db fehlt) mit auditierter Entscheidung (Recht ops.promote). NEU management/server/static/cockpit_promotion.js: IIFE + 'use strict'; DEV-Logging (AIW_COCKPIT_DEBUG); reine Helfer (allowedActions/reasonRequired/isFinal/statusDotClass/countsModel/candidateRows) als Spiegel der Server-Zustandsmaschine promotion_status.py; renderPromotion() zeigt Kennzahlen + Kandidatentabelle; Entscheidungs-Panel (herkunft optional, grund Pflicht bei zurueckgestellt/fremdzustaendig, Endzustand deutlich als endgueltig markiert), XSS-sicher via textContent; nur zulaessige Aktionen werden angeboten (Endzustand -> keine); UMD-Ausgang window.AIWCockpitPromotion. GEAENDERT management/server/static/cockpit.js: VIEW_CATALOG-Eintrag 'promotion' (ops.view, Gruppe Administration); loadPromotion() (GET /api/promotion; onDecide -> POST /api/promotion/decide mit X-AIW-Token; KEIN optimistisches UI - nach dem Schreiben Reload, Meldung via pendingMsg); selectView-Dispatch + SSE-'changed'-Reload (Beleg + Plattenmessung). GEAENDERT management/server/static/cockpit.html: <script> cockpit_promotion.js. GEAENDERT management/server/static/cockpit.css: eng gefasste .aiw-promo-* Stile (Tabelle, Buttons, gelbes Entscheidungs-Panel, rote Endgueltig-Warnung, Ergebniszeile). TESTS: tests/unit/test_cockpit_promotion.test.js (PP01-PP09: Helfer/Zustandsmaschinen-Spiegel, Rendern mit/ohne Recht, Grund-Pflicht im Panel, Endzustands-Warnung, leere Liste, XSS-sicher); GEAENDERT tests/unit/test_cockpit_nav.test.js (VIEW_CATALOG 23->24; ops.view zeigt nun integrity+promotion). REGRESSION: node --check OK; volle vitest 876 passed/1 skip/1 todo; volle pytest 1497 passed/49 skipped (unveraendert - reines Frontend). LIVE-VERIFIKATION IN DER VM EMPFOHLEN (Entscheidung im echten Browser: Panel, Grund-Pflicht, Reload, SSE). HINWEIS: Grant 'ops.promote' an supervisor per policy_admin/rbac_admin setzen (sonst nur lesend). Damit ist die Fremdforum-Promotion vollstaendig (460 Backend + 461 Frontend). Buildnummer ggf. manuell anpassen, falls 461 parallel vergeben."
}

{
  "build": 436,
  "version": "0.7.436",
  "date": "2026-07-19",
  "note": "Build 436 (Wartungsmodus - Webserver-Integration, Build B der Sequenz; ADDITIV bis auf minimale, getestete Haken; KEINE Schemaaenderung, migrationsfrei). NEU im maintenance/-Paket: gate.py (MaintenanceGate - Request-Gate mit Drain: enter/leave zaehlt In-Flight, block_and_drain blockt neue Requests -> HTTP 503 und wartet bis laufende ausgetrudelt sind, erst dann ist Bundle-Close sicher; unblock gibt frei); controller.py (MaintenanceController + Aktion - reine Entscheidungslogik: Normalserver quiesced bei aktivem Fenster [pause|beenden], resumed bei Fensterende, BEENDEN wenn eigener Build < min_build [Versions-Waechter]; --maintenance-Server laeuft normal, SELBSTBEENDIGUNG bei Fensterende, KILL auf Anforderung); poller.py (MaintenancePoller - Hintergrund-Thread, ruft je Aktion den passenden Callback, tick() ohne Thread testbar). GEAENDERT: server/http_server.py - maintenance_gate-Attribut (default None) und Gate-Wrap im zentralen _handle_request (bei blockiertem Gate HTTP 503 OHNE DB-Zugriff, sonst in-flight-gezaehlt), _send_503_wartung. main.py - neues Argument --maintenance; Schritt 5a Start-Guard (Start im Wartungsmodus NUR bei aktivem Fenster, sonst aktive Verweigerung -> Schutz vor feindlicher Uebernahme); Schritt 9c Integration (Gate am Server; ServerRegistration mit UUID bzw. PresenceBeacon; Poller mit Callbacks quiesce[block_and_drain -> Integrator stoppen -> Bundle schliessen -> ACK; bei beenden server.shutdown], resume[neues Bundle via ConnectionManager-Neuinstanz, Router NEU aufbauen da er das Bundle cached, Integrator neu, ACK entfernen, Gate unblock], Versions-Waechter, Selbstbeendigung, Kill); Schritt 10 finally raeumt Poller und Wartungsdateien auf und schliesst _rt.bundle [nach Resume aktiv]). GRENZE dokumentiert: langlebige Requests (offene SSE) trudeln nicht von selbst aus; block_and_drain laeuft dann in den Timeout (Default 30s) und faehrt mit Warnung fort - fuer garantierten Exklusivzugriff ist bei_aktivierung=beenden der verlaessliche Weg. Neue optionale Config: maintenance.poll_interval_sec (3), maintenance.drain_timeout_sec (30). TESTS: tests/test_maintenance_controller.py, 12 Faelle (Gate enter/leave/Blockade/Drain/Weckung; Controller pause->resume, beenden, Versions-Waechter, abgelaufenes Fenster, --maintenance Selbstbeendigung+Kill; Poller Voll-Ablauf mit Fake-Callbacks+Touch, unbekannte Aktion ohne Crash). Regression: GESAMTE pytest-Suite 1320 passed / 54 skipped, py_compile ueber gesamten Baum fehlerfrei, Import von main/http_server/maintenance ok. NAECHSTE SCHRITTE: Build C (Management-Integration), Build D (CLI maintenance.py + maintenance_kill.py)."
}

{
  "build": 345,
  "version": "0.7.345",
  "date": "2026-07-10",
  "note": "Build 345 (Baustelle 7, Welle 0, RBAC Schnitt c): Aufloesungs-/Durchsetzungsschicht. Rein LESEND, keine Schemaaenderung, kein CoordinatorWriter, kein Datenverlust-Risiko (coordinator.db ohnehin nur lesend). Autoritativ: Bauplan_Baustelle7_Management_v1_1 Paragraph 11.3, Bauplan_Build344_RBAC_Writepath Paragraph 8, management/Bauplan_Build345_RBAC_Resolver_v0_1.md; mc 2026-07-10 (fahre fort). KERN (1) NEU management/rbac/rbac_resolver.py mit PersonPolicy (frozen dataclass DTO: person_id, roles frozenset, capabilities dict cap->scope; Methoden can(cap), scope(cap)), RbacResolver (resolve(person_id), can(person_id,cap), scope_for(person_id,cap)) und den Fehlern RbacResolverError/RbacCatalogError sowie der Funktion verify_catalog_present(con). AUFLOESUNG (Paragraph 11.3): aktive Rollen der Person aus person_role (revoked_at IS NULL) -> Vereinigung der aktiven Grants dieser Rollen aus rbac_grant (revoked_at IS NULL) -> Faehigkeit gilt bei >=1 Grant, Scope = WEITESTER. Scope-Ordnung 'alle'(2) > 'eigene'(1) > None(0); None = kein Scope ausgewiesen (z.B. reports.approve), niedrigster Rang. default-deny: keine Rolle/kein Grant => keine Faehigkeit; unbekannte Person => leere Policy (KEIN Fehler). Kolokation von DTO/Fehlern/Resolver in einer Datei folgt der Praezedenz dashboard_repo.py (CaseOverview + DashboardSchemaError + DashboardRepo). START-CHECK verify_catalog_present (Paragraph 11.3 'jede Code-Capability existiert in der DB'): Richtung Code TEILMENGE DB - jede Rolle/Faehigkeit aus catalog.py muss in rbac_role/rbac_capability geseedet sein; die DB DARF voraus sein (neue Migration hat Codes ergaenzt, die der Code noch nicht kennt -> zulaessig). Fehlt etwas -> harter, handlungsleitender RbacCatalogError (Hinweis 'python -m management.migrate'), niemals stiller Durchgang (GR1). Fehlende RBAC-Tabellen (OperationalError) -> ebenfalls RbacCatalogError. Wird in management.py beim Start des Management-Servers verdrahtet (Welle 0, Schritt 3); hier eigenstaendig und testbar. NICHT an core/startup_checks.py (Baustelle-2-Forensik-Webserver) gehaengt - getrennte Belange. (2) NEU tests/test_management_rbac_resolver.py (S01-S11, 11 Tests): default-deny ohne Rolle; Einzelrolle mit Grants (inkl. Scope None); Scope-Widening (eigene+alle->alle, None<eigene); zurueckgenommener Grant/Rolle faellt raus; Mehrfachrollen-Vereinigung; ungegrantete Faehigkeit; Katalog-Check gruen; fehlende Faehigkeit/Rolle -> RbacCatalogError (handlungsleitend); DB voraus -> ok; Read-Only-Nachweis (Zeilenzahlen + audit_log unveraendert). ENTSCHEIDUNGEN (mc): Scope-Widening alle>eigene>None; Start-Check im rbac-Modul (nicht am Forensik-Webserver); Pruefrichtung Code TEILMENGE DB. BEWUSST NICHT ENTHALTEN: Verdrahtung in management.py (Management-Server + SSE = Welle-0-Schritt 3, Folge-Build); UI/Durchsetzung an Endpunkten. Damit ist die RBAC-Trias (Schema 343 / Schreibpfad 344 / Aufloesung 345) komplett. REGRESSION run_tests.py: Python 844 passed (833+11) / 59 skipped / 3 subtests, JavaScript 467 passed / 1 skipped / 1 todo (kein JS geaendert). DEPLOY: keine Migration, kein Schema-/Datenpfad beruehrt. HINWEIS: Bauplan-Detaildatei liegt im ZIP, per .gitignore (*.md) nicht getrackt -> 'git add -f'. GELIEFERT: siehe ZIP (per-file MD5)."
}

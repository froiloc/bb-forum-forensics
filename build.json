{
  "build": 443,
  "version": "0.7.443",
  "date": "2026-07-19",
  "note": "Build 443 (AP-2B Export-Vereinheitlichung, Baustein 4/4 - StA-Ausschleus-Verzeichnis; mc 2026-07-19). Definierter Sammel-/Uebergabepunkt Richtung Staatsanwaltschaft. REIN ADDITIV; coordinator.db wird - wenn ueberhaupt - NUR LESEND (Ketten-Spitze im Erzeugungsvermerk) herangezogen; kein Schemaeingriff, kein Migrationsvorbehalt. NEU management/export/staging.py (Klasse StagingArea, DB-frei, Dateisystem, Zeit injiziert -> testbar): add_artifact() nimmt gepruefte Dateien auf und verlangt AUSDRUECKLICH unbedenklich=True + cleared_by (default-deny -> UnbedenklichkeitError; nichts wird still uebersprungen, GR1 + Fallregel 3); je Artefakt SHA-256 (content_sha256_bytes), Groesse, Pruefer, Notiz, Zeit; Namenskollision -> StagingError (kein Ueberschreiben). manifest.json mit Artefaktliste + manifest_sha256 (json_payload_sha256 ueber die Artefaktliste, unabhaengig nachrechenbar); Zustand ueberlebt CLI-Aufrufe (inkrementell). finalize(context) stempelt Kopfdaten + Erzeugungsvermerk (via ExportEnvelope, inkl. Ketten-Spitze) und schreibt menschenlesbare UEBERGABE.txt (Aktenkopf + Artefakte mit Pruefsummen + Fuss). verify() rechnet jede Datei gegen das Manifest nach und meldet mismatched/missing/extra -> Paket selbst-pruefbar. NEU management/export/ausschleus_admin.py (CLI add/finalize/verify; --unbedenklich ist ein ausdruecklicher Schalter, --cleared-by Pflicht; finalize optional --coordinator-db nur lesend via context_builder). GEAENDERT management/export/__init__.py (+StagingArea/StagingError/UnbedenklichkeitError). TESTS tests/test_management_export_staging.py (ST01-ST10: Verweigerung ohne Unbedenklichkeit + Datei nicht aufgenommen, Kopie+sha256+size, Manifest-Digest aenderungssensitiv, Zustand ueberlebt neue Instanz, finalize stempelt+UEBERGABE.txt, verify ok/tamper/missing/extra, cleared_by Pflicht, UTF-8, Namenskollision). REGRESSION: py_compile OK; volle pytest-Suite gruen (siehe Handshake); CLI add/finalize/verify end-to-end geprueft. Damit ist AP-2B (Export-Vereinheitlichung) komplett (440 Framework, 441 Excel, 442 Retrofit je Sicht, 443 StA-Ausschleus). HINWEIS: Buildnummer ggf. manuell anpassen, falls 443 parallel vergeben."
}

{
  "build": 306,
  "version": "0.7.306",
  "date": "2026-07-01",
  "note": "Build 306 (Baustelle 7 — Management-Interface, Tag 1): Fundament 'Migrations-Geruest + Audit-Log + Write-Gateway' (Idee 13). NEU: Ordner management/ (geschuetzt, Dateisystem-Rechtevergabe). management/audit/hashing.py (eingefrorene Hash-Formel SHA-256 ueber prev_hash|seq|ts|actor_id|event_type|target_type|target_id|content|meta, Feldtrenner 0x1F; meta von Beginn an Teil der Formel -> kuenftige Zusatzinfo ohne Spalten-Rebuild). management/audit/event_types.py (eingefrorenes Enum: genesis, migration_applied, chain_verified). management/audit/audit_log.py (Klasse AuditLog: create_schema inkl. Append-only-Trigger, write_genesis, append [in aktiver BEGIN-IMMEDIATE-Tx], verify_chain). management/migrations/runner.py (Klasse MigrationRunner: forward-only, idempotent, wal_checkpoint(TRUNCATE) vor Migration nicht-fatal, BEGIN IMMEDIATE pro Migration -> sauberer Abbruch ohne Teilzustand bei nicht erlangbarer Sperre, checksum-Pruefung, schema_migrations-Registry). management/migrations/coordinator/m001_audit_log.py (M001, additiv/datenneutral: audit_log + Genesis). management/migrations/{evidence,assets}/ PLATZHALTER fuer spaeteren schema_migrations-Retrofit. management/gateway/coordinator_writer.py (Klasse CoordinatorWriter: bindet Write + Audit-Eintrag in EINE Transaktion -> kein Management-Write ohne Audit, kein Audit ohne Write). Kette nach M001: seq1=genesis, seq2=migration_applied(deployed_by). KEINE Daten-Migration (Dummies). Tag 1 fasst den Request-/Auslieferungspfad NICHT an (--user-id unveraendert). Tests tests/test_management_audit.py A01-A09 (Anlage+Idempotenz, Genesis+migration_applied, Verkettung+Hash-Nachrechnung, verify OK, Manipulationsnachweis bei Trigger-Umgehung, UPDATE/DELETE-Sperre, atomarer Rollback, meta-Reserve, sequentielle Appends). Volle Python-Regression pytest BESTANDEN (572 passed, 59 skipped, 0 Fehler). JS unveraendert. Beleg: Bauplan_Baustelle7_Management_v0_1.md v0.2, Projektgespraech 2026-07-01, mc 2026-07-01."
}

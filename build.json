{
  "build": 352,
  "version": "0.7.352",
  "date": "2026-07-10",
  "note": "Build 352 (Baustelle 7, Welle 0): BACKUP TEIL 1 - Konfiguration + Enumeration + Speicherplatz-Vorabpruefung (Fundament, rein pytest-testbar; noch KEIN Schreiben). Autoritativ: Bauplan_Baustelle7_Management_v1_1 Paragraph 11/7.5.3; management/Bauplan_Build352_BackupConfig_v0_1.md; mc 2026-07-10 (Split 352/353/354; Feldvorschlag uebernommen mit kommentierter config; default.db/templates.db/translations.db mitsichern). (1) GEAENDERT config.yaml: neue 'backup:'-Sektion mit AUSFUEHRLICHEN erklaerenden Kommentaren (dest_dir inkl. UNC-Beispiel; retention_count; min_free_factor vorfall-getriebene Platzreserve; checkpoint passive/none - nie truncate; include_shared_dbs). Zusaetzlich paths.translations_db explizit aufgenommen (wurde bisher nur per .get-Default gelesen). (2) GEAENDERT core/config_loader.py: _DEFAULTS um paths.translations_db und die 'backup:'-Defaults erweitert (dest_dir ./backups/, retention_count 7, min_free_factor 1.3, checkpoint passive, include_shared_dbs True). Zugriff via cfg.get('backup.*'). (3) NEU management/backup/__init__.py (Paket). (4) NEU management/backup/backup_config.py: BackupConfig (frozen dataclass) + from_loader() mit Typpruefung und klaren Fehlern (BackupConfigError). checkpoint 'truncate' wird ABGEWIESEN (im Backup-Pfad verboten); retention>=1; factor>0. (5) NEU management/backup/backup_planner.py: BackupSource/BackupPlan (DTOs) + BackupPlanner. enumerate_sources() ermittelt ALLE Quell-DBs: coordinator immer, shared (default/templates/translations) je include_shared_dbs, plus je '*.db' in forensic/evidence/assets (Label = Dateiname-Stamm, z.B. 'evidence_18'; Nicht-.db ignoriert). Fehlende Einzel-DBs UND fehlende Verzeichnisse werden im Plan sichtbar gemacht (missing-Liste, Grundregel 1 - kein stiller Fehlpfad). plan() fuehrt die SPEICHERPLATZ-VORABPRUEFUNG durch (vorfall-getrieben: default.db-Malformed durch volle Platte 2026-07-01): required_free = ceil(Gesamtgroesse * min_free_factor); freier Platz via shutil.disk_usage am Ziel ODER (genau eine Ebene) am direkten Elternverzeichnis (Ziel-Unterordner existiert evtl. noch nicht; nicht weiter hochlaufen, um die Pruefung nicht auszuhebeln). ok=False mit klarer Begruendung bei: unerreichbarem Ziel, keinen Quellen, zu wenig Platz. Rein lesend, kein VACUUM (Executor = Build 353). (6) NEU tests/test_backup_planner.py (BC01-BC03, BP01-BP07): Config-Typung + Fehlerfaelle (truncate/bounds), Enumeration (alle 7 DBs, .txt ignoriert), include_shared_dbs=False, Groessen/required_free, unzureichender Platz, fehlende Einzel-DB nicht fatal, unerreichbares Ziel, keine Quellen. REGRESSION run_tests.py: Python 894 passed (883+11) / 59 skipped / 3 subtests; JavaScript 511 passed (unveraendert) / 1 skipped / 1 todo (513), 40 Testdateien. NAECHSTE BUILDS: 353 Executor (VACUUM INTO je Plan-Eintrag via BackupTool, integrity_check auf Kopie, SHA512, Manifest, Retention-Pruning); 354 'backups'-Registry (additive Migration, naechste freie Nummer) + BACKUP_CREATED-Audit + backup_admin-CLI. HINWEIS: config.yaml + config_loader.py sind getrackt (kein Force-Add). Nur die Bauplan-.md ist per .gitignore untracked -> 'git add -f'. GELIEFERT: siehe ZIP (per-file MD5)."
}

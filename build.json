{
  "build": 370,
  "version": "0.7.370",
  "date": "2026-07-10",
  "note": "Build 370 (Baustelle 7, Welle 1): STATISTIKEN TEIL 1 - Backend /api/stats (StA/Fuehrung) + StatsRepo. Keine Migration. Split (mc 2026-07-10): Frontend (Reiterstruktur/Diagramme) -> Build 371. Autoritativ: Ideen_Verwaltungswerkzeug_konsolidiert Paragraph 2.4; DashboardRepo-Aggregat + audit_log; management/Bauplan_Build370_StatsBackend_v0_1.md; mc (Basis-Kennzahlen + Durchsatz ueber Zeit im ersten Schwung; CSV UND JSON-Export im Backend; scope alle umfasst zusaetzlich eigene). (1) NEU management/stats/__init__.py + stats_repo.py (StatsRepo): compute([person_id]) - rein lesende Kennzahl-Matrizen aus DashboardRepo.list_case_overview() + audit_log. Liefert totals (cases/assigned/unassigned/events), by_status (open/in_progress/approved/closed), by_priority ('1'..'5'), by_ampel, by_assignee ([{person_id, display_name, count}], nach Anzahl sortiert), throughput_by_day ([{day 'YYYY-MM-DD', count}] = Fall-Ereignisse je Tag aus audit_log target_type='case' = Auswertungs-DURCHSATZ), scope, generated_at. compute(person_id) filtert auf eigene zugewiesene Faelle (Durchsatz dann nur zu diesen Faellen). to_csv(stats) -> maschinenlesbares Langformat 'abschnitt,schluessel,wert' (Python csv, sichere Escapes). (2) GEAENDERT management/server/management_app.py: Response.csv-Fabrik (text/csv); CAP_STATS='stats.export_sta'; Import StatsRepo; Dispatch '/api/stats' -> _stats. _stats: cap-Check (403); scope-aware ('alle' -> compute(None) alle Faelle; 'eigene' -> compute(person_id)); Query 'format' json (Vorgabe) oder 'csv' (Response.csv). Read-only. Versionskopf -> 370. HINWEIS: Download-Dateiname kommt im Frontend (Blob); der gerichtsfeste/gepruefsummte StA-Export bleibt ein spaeteres eigenes Subsystem (Welle 3). (3) NEU tests/test_stats_view.py (ST01-ST05): Aggregate (totals/by_status/by_priority/by_assignee), Durchsatz aus audit_log, Endpunkt json + 403, CSV-Export (text/csv + Langformat-Header), scope eigene (nur eigene Faelle). REGRESSION run_tests.py: Python 965 passed (960+5) / 59 skipped / 3 subtests; JavaScript 545 passed (unveraendert - Backend-only) / 1 skipped / 1 todo (547), 46 Testdateien. ABNAHME: nach Grant stats.export_sta GET '/api/stats' -> {totals, by_status, by_priority, by_ampel, by_assignee, throughput_by_day}; GET '/api/stats?format=csv' -> text/csv Langformat. Supervisor (alle) alle Faelle; investigator (eigene) nur eigene. NAECHSTER BUILD 371 (Frontend, console-first): cockpit_stats.js mit REITER-STRUKTUR (Tabs) zwischen Tabellen/Diagrammen (ECharts: Status/Prioritaet/Ampel-Verteilungen, Durchsatz-Zeitreihe; Tabulator: by_assignee) + Download-Buttons (CSV via Endpunkt, JSON via In-Memory-Blob) + Verdrahtung + SSE. HINWEIS: nur die Bauplan-.md ist per .gitignore untracked -> 'git add -f'. GELIEFERT: siehe ZIP (per-file MD5)."
}

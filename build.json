{
  "build": 495,
  "version": "0.8.495",
  "date": "2026-07-21",
  "note": "Build 495 (PLATZHALTER-NEUORDNUNG, Slice 3, Teil 4: case-weite Wiederverwendung von m/o-Ermittlerwerten ueber placeholder_cache; mc-Wunsch 2026-07-20/21). Basis origin/master 1667f4c (v0.8.494). KEIN Schema-/Migrationseingriff: es wird ausschliesslich die BESTEHENDE Tabelle evidence_<uid>.db/placeholder_cache gelesen/geschrieben (dieselbe, die {{a:}} nutzt) -> migrationsneutral. ZWECK: Ein einmal eingetragener Wert eines BEKANNTEN m/o-Platzhalters (in templates.placeholders definiert) wird im Fall (uid) vorgehalten und kann in anderen Vermerken desselben Falls als Vorschlag uebernommen werden. BACKEND: db/evidence_db.py NEU get_cache_entries_for_ids(uid, ids) -> {id:value} (gezielt nur angefragte IDs, kein {{a:}}-Leak; leere Menge No-op). forensic_api/placeholders.py NEU handle_cache_get (GET /_forensic/placeholders/cache?ids=a,b -> {id:value}; ohne ids leeres Objekt) und handle_cache_set (POST {id,value} -> Writeback via set_cache_entry). SCHUTZ (Grundregel 1/Datenintegritaet): handle_cache_set laesst NUR bekannte Platzhalter vom Typ m/o zu (Pruefung ueber templates.get_query(id).type); ein 'a'-Auto-Platzhalter oder ein unbekannter Bezeichner -> HTTP 400, KEIN Schreibvorgang (kein Ueberschreiben des {{a:}}-Caches, keine Cache-Verunreinigung). forensic_api/__init__.py: Route /_forensic/placeholders/cache (GET+POST) im Placeholders-Dispatch ergaenzt. FRONTEND: NEU userinfo/placeholder_reuse.js (window.PlaceholderReuse): loadCache(ids) (GET, mergt in lokalen {id:value}-Cache), getCached(id), writeback(id,value) (POST, fire-and-forget, Erfolg aktualisiert lokalen Cache), isLoaded, _setForTest/_resetForTest. userinfo/placeholder_wizard.js: _saveField schreibt bekannte m/o-Werte case-weit zurueck (Writeback nur fuer PlaceholderDefs.get(name) && nicht-leer); NEU _loadDefsAndReuse (laedt erst die Definitionen, dann die case-weiten Werte der bekannten m/o-IDs der Sicht) ersetzt den 494-Defs-Ladeblock; NEU _applyReuseHints bietet fuer LEERE bekannte m/o-Felder den gecachten Wert als ANKLICKBAREN Vorschlag ('Zuletzt im Fall: <wert>') unter dem Feld an — ein Klick uebernimmt (loest Eingabe->Validierung/Propagation/Speichern inkl. Writeback aus) und entfernt den Hinweis. Der Vorschlag ist NICHT persistent bis zur Uebernahme (saubere Trennung 'vorgeschlagen' vs. 'eingetragen'). userinfo/report.css: Stile .pf-reuse-hint/.pf-reuse-apply. ROUTING (Konsequenz Build 493): placeholder_reuse.js in static.py (_RESOURCES) + __init__.py (Dispatch-Tupel) registriert und in report.py VOR placeholder_wizard.js eingebunden; der Waechter test_report_assets_routing.py deckt es ab. TESTS: tests/test_evidence_db_b6.py T27-T29 (get_cache_entries_for_ids: nur angefragte IDs, uid-Grenze, leere Menge). tests/test_placeholders.py T30-T36 (cache_get nur angefragte IDs / ohne ids leer; cache_set speichert m, lehnt a-Typ und unbekannte id und leere id ab (400, kein Schreiben); Writeback->Prefill Round-Trip). NEU tests/unit/test_placeholder_reuse.test.js (T01-T07: getCached, loadCache Merge/leer/Fehler, writeback POST/400/leere-id). tests/unit/test_placeholder_wizard.test.js T50-T53 (leeres Feld -> Vorschlag; befuelltes -> keiner; Klick uebernimmt+entfernt Hinweis; Save loest Writeback POST aus). REGRESSION: volle Python-Suite gruen unter Python 3.13 (1639 passed, 61 skipped, 6 subtests; +10); volle vitest-Suite gruen (1014 passed, 85 Dateien; +11); node --check + ast.parse OK; Routing-Waechter gruen. OFFEN aus 490/491: query_repo.py/query_validator.py per git rm entfernen (toter Code). STAND SLICE 3: DB-Migration+Backend (489), Management-Maske (490), Routing-Fix (493), Stammvater/Klon-Logik+Wizard (491/492), list/like-Validierung (494), case-weite m/o-Wiederverwendung (495) sind ausgeliefert. OFFEN/ZUR ENTSCHEIDUNG: Persistenz der Klon-Markierung ueber Neuladen (Migrationsvorbehalt, wartet auf deinen Entscheid); optionale Case-Insensitivitaet fuer list/like."
}

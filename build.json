{
  "build": 451,
  "version": "0.7.451",
  "date": "2026-07-19",
  "note": "Build 451 (AP-2F Steuerung/Eskalation, Baustein 1/3 - aktive Ueberlastwarnung; mc 2026-07-19). Macht aus der bisherigen reinen Ampelfarbe eine AKTIVE Warnung: wertet die je Ermittler gemessenen Lastzahlen (WorkloadRepo/InvestigatorLoad) gegen konfigurierbare Schwellen aus. REIN ADDITIV/nur-lesend; kein Migrationsvorbehalt. NEU management/workload/overload.py: OverloadThresholds (max_active_cases=10, max_red_cases=3, backlog_alert=5; via config.yaml workload.overload.* uebersteuerbar). assess_load(load, thresholds) = REINE Bewertung EINER Lastzeile mit einzeln benannten Ausloesern (GR1); Schwellenbedeutung eindeutig: 'warn' = Schwelle ERREICHT (==), 'overload' = UEBERSCHRITTEN (>). build_report(loads, thresholds, now) = reine Report-Bildung (dateilos testbar): Rueckstau (is_backlog) fliesst NICHT in die Personen-Bewertung, sondern in backlog_size/backlog_alarm (systemisches Signal); Sortierung overload>warn>ok, dann aktive absteigend; overloaded_count/warned_count. OverloadEvaluator(con).evaluate() zieht die Lasten aus WorkloadRepo und ruft build_report. overload_thresholds_from_config (voll abgesichert), overload_to_dict. NEU management/workload/overload_admin.py: NUR-LESENDE CLI (--json). TESTS tests/test_management_overload.py (OV01-OV08: ok/warn(erreicht)/overload(aktiv+rot) mit Ausloesern, Rueckstau nicht als Person + Alarm, Sortierung, Zaehlungen, config-Schwellen None-sicher). REGRESSION: py_compile OK; volle pytest-Suite gruen (siehe Handshake). NAECHSTE AP-2F-Bausteine: 452 Naechstbeste-Aktion-Warteschlange, 453 Eskalationsregeln. HINWEIS: Buildnummer ggf. manuell anpassen, falls 451 parallel vergeben."
}

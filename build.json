{
  "build": 315,
  "version": "0.7.315",
  "date": "2026-07-03",
  "note": "Build 315 (Baustelle 7 - Ampel-Dashboard: Backend-Haertung nach mc 2026-07-03). Setzt die drei bestaetigten Entscheidungen um; der gerenderte Admin-BILDSCHIRM bleibt BEWUSST Build 316, weil er Live-Browser-Abnahme braucht (Aufteilung testbares Backend jetzt / Frontend nach Abnahmemoeglichkeit). GEAENDERT management/dashboard/dashboard_repo.py: (1) Ampel-Schwellen NICHT mehr hartkodiert - neue Modulfunktion ampel_thresholds_from_config(cfg) liest dashboard.ampel.amber_idle_days / red_idle_days aus config.yaml (cfg = Objekt mit get(dotted_key, default), z.B. ConfigLoader); fehlen die Schluessel, greift die Vorgabe 7/21 NICHT still, sondern mit Log-Hinweis (Grundregel 1); ungueltige Werte (nicht-ganzzahlig, amber<1 oder red<=amber) -> harter, handlungsleitender DashboardConfigError statt stiller Fehlkonfiguration. (2) NEUE Sortierung (mc): Ampel-Schwere zuerst (_AMPEL_RANK rot<gelb<gruen), dann Prioritaet aufsteigend, dann letzte Aktivitaet absteigend, dann user_id. (3) Handlungsleitende Fehler: neue _check_required_tables()-Preflight vor der Aggregatabfrage; fehlt eine Pflichttabelle (cases, case_events, support_sessions, investigators) -> DashboardSchemaError mit Klartext 'Tabelle X fehlt ... python -m management.migrate' statt rohem sqlite3.OperationalError. Neue Exceptions DashboardSchemaError/DashboardConfigError. Support-Praesenz bleibt aus der Farbe heraus (eigenes Abzeichen). Alle vormals 'PROVISORISCH/mc ausstehend'-Kommentare durch bestaetigte Belege ersetzt. GEAENDERT management/dashboard/dashboard_admin.py: laedt config.yaml EINMAL (_load_config), _resolve_db_path nutzt denselben Loader; baut Schwellen via ampel_thresholds_from_config; faengt DashboardSchemaError/DashboardConfigError ab und gibt die handlungsleitende Meldung + Exit 1 aus (kein Traceback); Fusszeile zeigt aktive Schwellen. GEAENDERT management/dashboard/__init__.py (Versionskopf). GEAENDERT tests/test_management_dashboard.py: D13 (Schwellen aus config: explizit, fehlend->Vorgabe, None->Vorgabe, ungueltig amber>=red / nicht-ganzzahlig / amber<1 -> DashboardConfigError), D14 (Sortierung Ampel-Schwere zuerst ueber rot/gelb/gruen mit deterministischem _set_activity + injiziertem now; innerhalb ROT juengste Aktivitaet zuerst), D15 (fehlende case_events-Tabelle -> DashboardSchemaError, Meldung nennt Tabelle UND migrate). WICHTIG - config.yaml wird BEWUSST NICHT im ZIP mitgeliefert, um die Prod-config.yaml (UNC-Pfade) nicht zu ueberschreiben; der optionale Abschnitt ist zu ergaenzen: 'dashboard:\\n  ampel:\\n    amber_idle_days: 7\\n    red_idle_days: 21'. Ohne den Abschnitt laeuft alles mit Vorgabe 7/21. KEINE Migration, kein Schreibpfad, nur-lesend auf coordinator.db - kein Datenverlust-Risiko. Regression run_tests.py: Python 655 passed / 59 skipped / 3 subtests (war 652), JavaScript 408 passed (unveraendert - kein Frontend beruehrt). GEAENDERT management/Bauplan: v0_9 abgeloest durch Bauplan_Baustelle7_Management_v1_0.md (neues Paragraph 10 = Build 315; Paragraph 10.6 = Architekturvorschlag Build 316 getrennte Admin-Oberflaeche als eigener localhost-Admin-Server, mc ausstehend). OFFEN/ENTSCHEIDUNG: Build 316 - (a) getrennter Standalone-Admin-Server auf eigenem localhost-Port als Form der 'sauberen Trennung' bestaetigen; (b) ab wann Live-Browser-Abnahme moeglich. OFFEN weiterhin: Live-Browser-Verifikation Build 312. HINWEIS Repo-Stand: GitHub-HEAD webserver stand zu Beginn auf 313; Build 314 war nur per ZIP nach /opt ausgerollt, nicht committet - 314 und 315 sind noch nach GitHub zu committen. Prepper GitHub-HEAD 0.1.102 (alte 101/102-Diskrepanz aufgeloest). Beleg: Bauplan B7 v1.0 Paragraph 10, mc 2026-07-03."
}

{
  "build": 348,
  "version": "0.7.348",
  "date": "2026-07-10",
  "note": "Build 348 (Baustelle 7, Welle 1): COCKPIT OVERVIEW-SICHT + SSE-Reload. Reiner Frontend-Build (kein Backend/keine Migration); baut auf 347-Shell auf. console-first vorab: PoC im echten Browser bestaetigte Tabulator v6.4.0 (tableBuilt, DOM-Zeilen ok) und SSE (hello/keepalive) GRUEN; Overview-Datenform ist autoritativ aus dem Backend (_overview, 346) bekannt. Split (Entscheidung Alex): Integritaets-/Ops-Sicht -> eigener Build 349. Autoritativ: Bauplan_Baustelle7_Management_v1_1 Paragraph 11.2 (Tabulator, SSE-Reload) + 11.1 (Live via SSE, alle schlaegt eigene); management/Bauplan_Build348_CockpitOverview_v0_1.md; mc 2026-07-10. (1) NEU management/server/static/cockpit_overview.js (IIFE, DEV-Debug abschaltbar, ausfuehrlich kommentiert, reine Funktionen UMD-exportiert -> vitest testet ECHTEN Code; window.AIWCockpitOverview): reine ampelRank/reasonLabel/assigneeLabel/supportLabel/daysSince/toRows/sortRows/columnDefs/scopeText; DOM renderOverview() als Tabulator-v6-Tabelle. KONVENTIONS-VERTRAG: Ampel-Vokabular (rot>gelb>gruen), ampel_reason-Labels und Anzeige-Sortierung (Ampel-Schwere -> Prio aufsteigend -> letzte Aktivitaet absteigend -> user_id) spiegeln dashboard_repo.AMPEL_* (Build 315)/dashboard.js; bewusst eigenstaendig+eigenstaendig getestet (Split-Build, keine Kopplung an die getrennte Dashboard-Auslieferung). 10 Spalten (Ampel-Farbpunkt+Grund, Prio, User-ID, Benutzer, Status, Zugewiesen, Letzte Aktivitaet, Ereignisse, Notiz, Support), sortier-/filterbar. XSS-sicher: Textspalten via Tabulator-'plaintext' (textContent), eigene Formatter bauen DOM-Knoten (kein innerHTML mit variablem Text). Tabulator-Ctor injizierbar (Testbarkeit); nowSec injizierbar. (2) GEAENDERT management/server/static/cockpit.js: selectView verzweigt (dashboard -> loadOverview, sonst Platzhalter); loadOverview holt /api/overview und rendert via AIWCockpitOverview; state.table + destroyTable() (Tabulator-Lifecycle: alte Instanz vor Neuaufbau/Sichtwechsel abbauen -> keine Leaks/Doppel-Tabellen); renderError() (sichtbarer Fehlerhinweis, kein stiller Fehlpfad). SSE-Client startSse(): EventSource /events; bei 'changed' NUR die aktive Sicht neu laden (Overview), hello/keepalive = Lebenszeichen, onerror tolerant (Auto-Reconnect). boot() zeigt die erste Sicht ueber selectView (laedt ggf. Overview) und startet SSE. Auto-Boot weiterhin nur im echten Browser (window+document+fetch). (3) GEAENDERT management/server/static/cockpit.html: cockpit_overview.js vor cockpit.js eingebunden (defer, Reihenfolge). Integritaets-Banner bleibt neutral (Bindung folgt 349). (4) GEAENDERT management/server/static/cockpit.css: dezente Ampel-Zeilenfaerbung (.tabulator-row.aiw-row-rot/gelb/gruen via box-shadow). (5) NEU tests/unit/test_cockpit_overview.test.js (OV01-OV10, JSDOM, window.AIWCockpitOverview): reine Funktionen, sortRows-Reihenfolge+Nichtmutation, toRows abgeleitete Felder+Nichtmutation, columnDefs (10) + Ampel-Formatter (Farbpunkt+Grund), renderOverview (Kopf/Scope/Count, Stub-Tabulator erhaelt sortierte Zeilen+Spalten, ohne Ctor -> null+Hinweis). BEWUSST NICHT IN 348 (Build 349): Integritaets-/Ops-Sicht (/api/integrity) + Banner-Bindung. REGRESSION run_tests.py: Python 865 passed (unveraendert - reines Frontend) / 59 skipped / 3 subtests; JavaScript 487 passed (477+10) / 1 skipped / 1 todo (489), 36 Testdateien (35+1). BROWSER-ABNAHME (console-first Roll-out): Cockpit neu laden -> 'Dashboard' zeigt die Fall-Uebersicht als Tabulator-Tabelle (Ampel-Sortierung, Scope-Banner alle/eigene, Spalten sortier-/filterbar). SSE-Test: eine auditierte Aenderung erzeugen (z.B. rbac_admin grant/revoke) -> Overview laedt sich ohne F5 neu. HINWEIS: Bauplan-Detaildatei im ZIP, per .gitignore (*.md) nicht getrackt -> 'git add -f'. GELIEFERT: siehe ZIP (per-file MD5)."
}

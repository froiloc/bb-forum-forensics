{
  "build": 309,
  "version": "0.7.309",
  "date": "2026-07-01",
  "note": "Build 309 (Baustelle 7 — Wartung): NEUES Werkzeug zur VERLUSTFREIEN Konsolidierung mehrerer default.db. Hintergrund: der fruehere Prepper-Workflow legte das Output-Verzeichnis vor jeder User-Extraktion neu an -> pro Beschuldigtem eine eigene default.db (jeweils nur Teil-Snapshot der nutzerneutralen, forum-weiten Assets). Korrekt ist EINE zentrale default.db fuer alle Ermittler. Da default.db zur Laufzeit read-only ist (db/default_db.py, forensic_api/knownusers.py liest nur), existiert KEIN Ermittler-Wissen darin, das verloren gehen koennte — der Merge ist reine Vereinigung der Prepper-Ausgaben. Ablage bewusst im Webserver (nicht Prepper), weil das Werkzeug in der Produktivumgebung gebraucht wird, wo nur der Webserver vorliegt (mc 2026-07-01). NEU: management/maintenance/__init__.py; management/maintenance/default_db_merger.py (Klasse DefaultDbMerger + Wertobjekte TableStat/ConflictRecord/SourceInfo/MergeReport, eingebettetes kanonisches 10-Tabellen-Schema inkl. host_aliases gem. default_db_schema.sql); management/consolidate_default_db.py (CLI-Einstiegspunkt: python -m management.consolidate_default_db --target ... --source ... [--sources-glob] [--overwrite] [--allow-host-mismatch] [--report]); tests/test_default_db_merger.py (12 Tests M01-M11). Merge-Strategien: default_assets content_hash-Dedup mit AUTOINCREMENT-Remap (alt->neu-Map); default_urls url-Dedup mit asset_id-Remap ueber die Map (FK-Erhalt); known_aliases Dedup ueber (user_id,name), alias_id neu vergeben; default_meta stabile Keys protocol/domainname MUESSEN uebereinstimmen (sonst Abbruch, --allow-host-mismatch ueberstimmbar), Lauf-Keys last_run_* -> neueste Quelle gewinnt; uebrige Tabellen natuerlicher PK, identisch->dedup, abweichend->Konflikt (neueste Quelle gewinnt, protokolliert). Forensik: Quellen strikt read-only geoeffnet (Immutabilitaet); ganzer Merge in EINER Transaktion (Rollback bei Fehler); fail-loud bei unbekannter Tabelle/Spalte in Quelle (nie stilles Verwerfen); Invariante je Tabelle rows_read==inserted+deduped+conflicts+anomalies wird geprueft; abschliessend PRAGMA foreign_key_check (validiert asset_id-Remap); Herkunft in default_meta.merge_provenance (JSON) + append-only Abgleichbericht. Konfliktpolicy 'neueste Quelle gewinnt' via Verarbeitung der Quellen aufsteigend nach default_meta.last_run_ts. KEINE Aenderung an bestehenden Dateien -> keine Migrationsrelevanz fuer evidence_/forensic_/assets_<uid>.db. Regression Python (run_tests.py --python-only): 589 passed (577+12 neu)/59 skipped/0 Fehler. JavaScript-Suite unveraendert (kein JS geaendert) — in der VM per 'python run_tests.py' zu bestaetigen. Beleg: Analyse default.db-Konsolidierung 2026-07-01, mc 2026-07-01."
}

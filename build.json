{
  "build": 460,
  "version": "0.7.460",
  "date": "2026-07-20",
  "note": "Build 460 (AP-2G Betrieb/Governance, Fremdforum-Promotion-Zustandsmaschine, Backend; mc 2026-07-20; MD5-Handshake bestaetigt). Idee 25 (data/-Uebersicht/Fremdforum-Promotion) / Fundament F3. Ein FREMDFORUM-KANDIDAT ist ein Fall mit forensic_<uid>.db, aber OHNE evidence_<uid>.db (Beleg storage_overview.py:9-13,155-163). Build 454 lieferte die Kandidaten rein lesend; B460 gibt ihnen eine BELEGTE Entscheidung (Grundregel 1: keine stille Uebergehung). NEU management/ops/promotion_status.py: reine Zustandsmaschine (keine DB, keine Uhr) - Zustaende gesichtet/uebernommen/zurueckgestellt/fremdzustaendig; 'offen' ist die implizite Eingangslage (nie gespeichert); 'uebernommen'/'fremdzustaendig' sind ENDGUELTIG; zurueckgestellt->gesichtet (Wiederaufgriff) erlaubt; Grund-Pflicht bei zurueckgestellt/fremdzustaendig. NEU management/ops/promotion_repo.py: auditierter Schreibpfad ueber CoordinatorWriter (Write+audit_log in EINER Transaktion), Sensibilitaetsregel (Freitexte grund/herkunft NICHT im Audit-Payload, nur Textlaenge), allowed_uids-Gate (keine Entscheidung fuer Nicht-Kandidaten), KEIN case_events-Spiegel (ein 'neu'-Kandidat hat keine cases-Zeile - FK braeche). NEU management/ops/promotion_admin.py: CLI (candidates/list/decide, --force/--actor). NEU management/migrations/coordinator/m015_promotion.py: additive Migration forum_promotion (CHECK auf status, UNIQUE(user_id), KEIN cases-FK - Kandidat ist ggf. 'neu') + RBAC-Seed 'ops.promote' (literal, nicht aus catalog.py). GEAENDERT management/rbac/catalog.py: Faehigkeit 'ops.promote' (default-deny; Lesen haengt an ops.view). GEAENDERT management/audit/event_types.py: PROMOTION_DECIDED (+ALL). GEAENDERT management/server/management_app.py: GET /api/promotion (ops.view; Kandidaten-Scan via StorageOverview + Zustand aus forum_promotion) und POST /api/promotion/decide (ops.promote, auditiert; Kandidaten-Scan liefert allowed_uids). TESTS: test_promotion_status.py (PS01-06), test_promotion_repo.py (PR01-06), test_migration_m015_promotion.py (PM01-05); GEAENDERT test_management_rbac_schema.py (23->24 Faehigkeiten, +ops.promote) und test_management_dashboard.py (Migrationsliste ..14->..15). REGRESSION: py_compile OK; volle pytest 1497 passed/49 skipped; volle vitest 867 passed/1 skip/1 todo. LIVE-VERIFIKATION IN DER VM EMPFOHLEN (Promotions-Entscheidung im echten Browser folgt mit dem Frontend-Build 461). HINWEIS: Grant 'ops.promote' an supervisor per policy_admin/rbac_admin setzen (default-deny). Buildnummer ggf. manuell anpassen, falls 460 parallel vergeben."
}

{
  "build": 343,
  "version": "0.7.343",
  "date": "2026-07-10",
  "note": "Build 343 (Welle A der Userinfo-Verschoenerung, migrationsfrei, reiner Webserver-Build). Autoritativ: Bauplan_Userinfo_Verschoenerung v0.2, mc 2026-07-10. Umgesetzt: Pkt.3 (Karten-Homogenisierung), Pkt.4 (echter Benutzername + Gruppe im Kopf), Pkt.5 (Inhaltsverzeichnis mit Anker-Spruengen). Pkt.7 (Tabulator-Blaetter-Buttons) NICHT in diesem Build: Browser-JS-Bug -> Console-First-Protokoll, Diagnose vor Fix (separat geliefert). KERN: (1) db/forensic_db.py: neue READ-ONLY-Methode get_user_profile() liest username/group_id/group_details_json aus fdb.uid_profile (LIMIT 1). Kein JSON-Parsing hier (reiner Datenzugriff). Fehlt uid_profile -> None mit WARN-Log (GR1, kein stiller Fallback). (2) forensic_api/userinfo.py: handle() bezieht den ECHTEN Benutzernamen aus uid_profile.username statt des Platzhalters 'uid_<id>' (context.username war leer -> uid_-Fallback). Neuer Modul-Helfer _resolve_group_display() baut die Gruppenanzeige aus group_details_json (g_user_title > g_title > 'Gruppe <id>'), bereits HTML-escaped, defektes JSON degradiert auf ID. Kopf: H1=Benutzername, neue Zeile 'Gruppe: ...' ersetzt die nutzlose 'Benutzer-ID'-Zeile. KEIN Sonderfall fuer Gruppe 110 (Nachtrag 5: Arbeits-DB traegt Originalgruppe). Neuer <nav id='ui-toc' hidden>-Container im Kopf. CSS im inline-<style>: .ui-body jetzt zentriert (max-width 1200px, margin:0 auto) analog #userinfo-static statt linksbuendig 1100px; .ui-group und .ui-toc ergaenzt; Sperr-Banner auf die zentrierte 1200px-Spalte angeglichen. userinfo.css UNVERAENDERT. (3) userinfo/userinfo.js: neue Funktion buildTableOfContents() erfasst beide Kartensorten (.ui-body .ui-card via <h2> und #userinfo-static details.forensic-section via <summary>), vergibt eindeutige Anker-ids (Slug + Kollisions-Suffix), baut #ui-toc-Links, delegierter Klick-Handler scrollt sanft und klappt geschlossene <details> auf. Aufruf am Ende von loadStaticBlob() (nach BLOB-Load, damit BLOB-Karten existieren). Anker bewusst per JS statt im versiegelten BLOB -> migrationsfrei. DEV-Logging via _dbg. TESTS: neue pytest-Klassen TestUserinfoHeaderReform (7) + TestResolveGroupDisplay (6) in tests/test_baustelle4.py; neue tests/unit/test_toc_build.test.js (5) evaluiert die ECHTE buildTableOfContents-Funktion per Klammer-Matching aus der Datei (Anti-'gruen aber tot' B4-S12). REGRESSION run_tests.py: Python 827 passed / 59 skipped / 3 subtests (Baseline 814/59, +13); JavaScript 472 passed / 1 skipped / 1 todo (Baseline 467, +5). py_compile + node --check ok. GELIEFERT (ZIP, per-file MD5): db/forensic_db.py, forensic_api/userinfo.py, userinfo/userinfo.js, tests/test_baustelle4.py, tests/unit/test_toc_build.test.js, build.json. OFFEN: Pkt.7 Console-First-Diagnose (separat), danach Welle B (Pkt.1,2,6) im aiw_sqlite_prepper."
}
{
  "build": 496,
  "version": "0.8.496",
  "date": "2026-07-22",
  "note": "Build 496 (KORREKTHEIT: Freigabe-Button 'Zur Abnahme freigeben' reaktiviert). NUR FRONTEND (report_editor.js) + 1 neuer Test; kein Python-/DB-/Schema-/Migrationseingriff -> migrationsneutral. Basis origin/master e16370a (v0.8.495); report_editor.js ist zwischen 494 und 495 unveraendert (per git diff bestaetigt), das 496-Fundament stimmt. BEFUND (Console-Diagnose 2026-07-22, belegt): Der Knopf 'Zur Abnahme freigeben' und _submitReport lagen in userinfo/report.js. Diese Datei wurde bereits mit Build 100 durch report_editor.js im HTML-Template abgeloest und wird NICHT mehr geladen — Resource-Timing zeigt: report.js wird nie angefordert; window.Report=undefined; #btn-submit-report-slot und #btn-submit-report existieren nicht. Der in Build 382 dort ergaenzte Knopf war damit im Produktivbetrieb wirkungslos. Build 493 hatte nur das Routing von submit_dialog.js (dem Dialog) repariert, nicht den Knopf, der ihn oeffnet. GEAENDERT userinfo/report_editor.js: (1) Slot <span id='btn-submit-report-slot'> in die Selector-Leiste (initReportSelector) ergaenzt. (2) NEU _renderSubmitButton(): rendert den Knopf in den Slot, nur beim EIGENEN Vermerk im Status 'draft' (SubmitDialog.canSubmit: created_by === Benutzername && status === 'draft'); Benutzername aus #report-editor-body[data-username]; leert den Slot vor jedem Rendern (kein Doppel-Knopf). Klick oeffnet den zweistufigen Bestaetigungsdialog (submit_dialog.js) mit onConfirm -> _submitReport. (3) NEU _submitReport(reportId): reicht ueber den bestehenden Lock-gesicherten Pfad _docSend('submit_report', {report_id}) ein (Server-Handler action=='submit_report', draft->submitted, verlangt Lock + prueft Verfasser/Status erneut, Build 381); zeigt die Server-Klartextmeldung via _selectorStatus und ruft initReportSelector(reportId) auf, damit Status/Sperre/Knopf neu aufgebaut werden (Knopf verschwindet nach 'submitted'). (4) _renderSubmitButton() wird am Ende von _loadReportImpl nach JEDEM Laden aufgerufen (Status kann sich geaendert haben). (5) Export window.ReportEditor._renderSubmitButton + _setCurrentReportForTest (Test gegen echten Code). Verwendete CSS-Klassen (editor-btn/editor-btn-primary/status-msg-ok) existieren bereits -> kein CSS-Eingriff. TESTS: NEU tests/unit/test_submit_button.test.js (SB01-SB06: eigener draft -> Knopf; fremd -> keiner; submitted -> keiner; Klick oeffnet Dialog mit Titel+Callback; ohne SubmitDialog kein Knopf; erneutes Rendern kein Doppel-Knopf) — evaluiert report_editor.js + submit_dialog.js im echten JSDOM (kein Nachbau). canSubmit selbst ist bereits durch test_submit_dialog.test.js (SD02) abgedeckt. REGRESSION: volle vitest-Suite gruen (1020 passed, 86 Dateien; +6); volle Python-Suite gruen (1639 passed, unveraendert — kein .py angefasst); node --check OK. HINWEIS ZUR VERIFIKATION (VM): Bitte den End-to-End-Fluss (Knopf sichtbar bei eigenem draft -> Klick -> Dialog -> Freigabe -> Status 'submitted', Knopf verschwindet) in der VM bestaetigen; die Unit-Tests decken Rendern/Gating/Dialog-Oeffnen ab, den Lock-POST + Reload prueft am Ende die laufende Umgebung. NAECHSTER SCHRITT: 497 (Case-Insensitivity fuer Validierung: neue Spalte templates.db.placeholders.validation_ci, checkTyped ci-Parameter fuer regex/list/like, Management-Maske + PlaceholderDefs) — bewusst als eigener Build, da er eine templates.db-Migration mitbringt und isoliert verifiziert werden sollte."
}

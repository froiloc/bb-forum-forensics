{
  "build": 513,
  "version": "0.8.513",
  "date": "2026-07-24",
  "note": "Build 513 (AP-2F / Idee 21: UEBERLASTWARNUNG SICHTBAR, Backend). Basis v0.8.512 (f9d6418). BEFUND (Uebergabe 440-453): das Read-Model aus Build 451 (management/workload/overload.py) existierte als Repo + CLI, war aber in KEINER Cockpit-Sicht erreichbar - in management_app.py gab es dazu weder Import noch Endpunkt. ENTWURFSENTSCHEIDUNG: KEINE eigene Sicht, sondern Anbindung an die BESTEHENDE Lastverteilung '/api/workload'. Begruendung: die Warnung ist eine BEWERTUNG genau der Zahlen, die dort ohnehin stehen; eine zweite Sicht haette dieselben Zahlen ein zweites Mal geholt und koennte - bei zwei Abfragen zu zwei Zeitpunkten - einen ANDEREN Stand zeigen als die Sicht daneben. Das waere ein widerspruechlicher Beleg. GEAENDERT management/server/management_app.py: Import von build_report/overload_to_dict/overload_thresholds_from_config/OverloadThresholds; NEU _overload_thresholds() (config.yaml workload.overload.*, bei Konfig-Ausfall Vorgaben aus Build 451 PLUS logger.warning - kein stiller Zustand); _workload() bestimmt jetzt EINEN Zeitstempel 'now' und reicht ihn an list_workload(now=now) UND an build_report - Warnung und Balken koennen nicht auseinanderlaufen (belegt durch UB08). Der Report wird NACH dem Scope-Filter gebildet: wer nur die eigene Zeile sehen darf, bekommt auch nur eine Warnung ueber sich selbst; der Rueckstau (investigator_id 0) faellt durch denselben Filter, backlog_size ist dann 0. Das ist KEIN Leerbefund 'kein Rueckstau', sondern ein NICHT ERHOBENER Wert - die Antwort weist das ueber overload.scope_limited aus, damit die Sicht es benennen kann (Grundregel 1). ANTWORTFORM (rein additiv, keine bestehende Zusage geaendert): 'overload' traegt NUR Skalare (angewandte Schwellen + Zaehler + scope_limited), die Einzelbewertungen stehen FLACH daneben in 'overload_assessments'. Grund: der Akten-Export (511/512) rendert eine flache Abbildung als Schluessel-Wert-Tabelle und eine Zeilenliste als Tabelle - verschachtelt stuenden die Bewertungen als JSON-Klumpen in EINER Zelle. Es ist KEINE Doppelablage: 'assessments' wird per pop aus dem Block entfernt (belegt durch UB02). Die angewandten Schwellen fahren IMMER mit, damit jede Einstufung nachrechenbar ist. GEAENDERT management/export/view_export_catalog.py: workload-Spec bekommt die Abschnitte 'overload' und 'overload_assessments' VOR 'loads' - sie waeren ueber den Auto-Modus (Regel 2) ohnehin unter 'Weitere Daten' erschienen, hier bekommen sie einen benannten Platz in der Lesereihenfolge (erst der Alarm, dann die Last). TESTS NEU: tests/test_management_workload_overload.py (UB01-UB10; UB04/UB05 leiten die Fallzahl aus der ANTWORT ab statt sie hart zu kodieren, damit sie auch bei geaenderter config.yaml gueltig bleiben; UB08 belegt die Deckungsgleichheit von Warnung und Balken in BEIDE Richtungen; UB10 prueft Katalog-Zusage UND gerendertes Dokument). REGRESSION: Python 1815 passed / 61 skipped / 6 subtests (Basis 512 auf DERSELBEN Umgebung gemessen: 1805/61 - also +10; kein bestehender Test musste angefasst werden); vitest unveraendert 1080 passed / 91 Dateien (kein JS in diesem Build); py_compile aller geaenderten .py OK. KEINE MIGRATION, KEINE neue Faehigkeit (workload.view gilt weiter), KEIN neuer Nav-Eintrag (Kataloglaenge bleibt 32). HINWEIS ZUR PRUEFUMGEBUNG: hier mit Python 3.13 gemessen (3.14 in der Bauumgebung nicht verfuegbar); die abweichende Skip-Zahl gegenueber build.json 512 (61 statt 49) ruehrt aus versionsabhaengigen Skips, NICHT aus diesem Build - die Basis wurde vor der Aenderung auf derselben Umgebung mit 1805/61 gemessen. Bitte in der VM (3.14) gegenpruefen. OFFEN (kein stiller Verzicht): das Frontend zeigt die Warnung noch NICHT an - das ist Build 514. NAECHSTES ARBEITSPAKET: Build 514 (Ueberlastwarnung in cockpit_workload.js sichtbar machen)."
}

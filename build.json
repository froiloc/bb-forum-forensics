{
  "build": 483,
  "version": "0.8.483",
  "date": "2026-07-21",
  "note": "Build 483 (Tabulator-Umbau der Chef-Freigabe-Sicht W5, analog Lektorat 481/482; Frontend-only, rein lesend; Bauplan management/Bauplan_LektoratTabulator_v0_1.md §8/Reichweite, mc-Freigabe). Setzt auf origin/master HEAD 05c7719 (v0.8.481) auf; cockpit_approval.js unveraendert seit Build 479, Basis-MD5 ef6223777d2f22979de3c4df340f89d9, cockpit.css f12c20363fa0be6b54ee278f3e5468f2. ZWECK: Die Auswahl-Liste der Chef-Freigabe (bisher ein <button> je Bericht, unbegrenzt) wird durch eine Tabulator-Tabelle ersetzt — gleiches Look-and-feel wie die Lektorat-Sicht (Builds 481/482). GEAENDERT management/server/static/cockpit_approval.js: (1) reine Helfer toRows(data,status)/typeLabel/fmtTs/statusCounts (vitest-pruefbar; fehlende Felder sichtbar ersetzt, Grundregel 1); (2) renderApproval baut statt der Button-Liste eine Tabulator-Tabelle mit Spalten Benutzer/Titel/Typ/Nr./Status/Verfasser/Erstellt, Freitext-Header-Filtern (Benutzer/Titel/Typ/Status/Verfasser), Paginierung 'local' 20 Zeilen/Seite + deutschem Pager (locale de-de), height:false, placeholder bei 0 Zeilen; rowClick -> gemeinsame Auswahl-Logik _selectReport (setzt selKey, laedt read-only iframe-Vorschau, baut das Aktionsfeld _buildActionPanel(r) fuer DIESEN Bericht [nutzt status/subject_id/id — alle in der Zeile vorhanden], stoesst onSelect fuer Belege/Kommentare/Ergebnis an); (3) Statusfilter tauscht die Tabellendaten via table.replaceData() OHNE Neu-Render (Nebeneffekt: der frueher hier vorhandene Verlust von onSelect beim Neu-Render entfaellt); (4) Status-Schnellfilter mit Trefferzaehlern (statusCounts); (5) modul-interne Instanzverwaltung _state.table + _destroyTable, Abbau in cleanup() (cockpit.js:cleanupView ruft AIWCockpitApproval.cleanup()) und defensiv am Kopf von renderApproval; (6) API um toRows/typeLabel/statusCounts erweitert. KEIN Uebernahme-Knopf in der Chef-Freigabe (nur Lektorat) -> keine Knopf-Verlegung. GEAENDERT cockpit.css: Stile .aiw-approval-table + aktive Zeile. GEAENDERT tests/unit/test_cockpit_approval.test.js: AP03-AP07/AP09 auf injizierten Stub-Tabulator + rowClick umgestellt; AP03 prueft zusaetzlich headerFilter + paginationSize 20; NEU AP16 (toRows), AP17 (statusCounts), AP18 (Zaehler in den Filter-Optionen + replaceData). cockpit.js NICHT geaendert (Modul verwaltet die Tabelle selbst; hasSelection/SSE-Schutz aus Build 479 bleibt). Kein Server-/DB-/Schema-/Migrationseingriff, migrationsneutral. REGRESSION: volle vitest-Suite gruen; node --check cockpit_approval.js OK. Keine .py/.php-Aenderung -> keine py_compile. HINWEIS: cwd/Container-Vorsicht — sauber auf origin/master 05c7719 (481) aufgesetzt und MD5-verifiziert. Damit sind Lektorat (W4) und Chef-Freigabe (W5) auf dasselbe Tabellen-UX umgestellt."
}

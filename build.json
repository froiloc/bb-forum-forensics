{
  "build": 403,
  "version": "0.7.403",
  "date": "2026-07-14",
  "note": "Build 403: De-Duplizierung der {{a:}}-Platzhalter-Aufloesung (Baustelle 6/7, Restpunkt (a) aus Build 399/402). Autoritativ: mc 2026-07-14 ('403 bitte'). PROBLEM: Der Pfad Cache->Query-Definition->SQL(fdb)->Cache existierte ZWEIMAL — in forensic_api/placeholders.py (_resolve_body.replace_match + _execute_query) und in report_render/report_source.py (_resolve_auto). Zwei Implementierungen desselben Verhaltens sind die sicherste Art, sie auseinanderlaufen zu lassen. NEU report_render/auto_query.py: AutoQueryResolver ist ab jetzt die Single Source of Truth. resolve(query_id, uid) -> AutoResult(value, status) mit status cache_hit|resolved|empty|no_query|sql_error; execute_query(sql, uid) -> (value_or_None, ok) fuer den refresh-Pfad; resolve_value_or_none() als Adapter fuer den Export. VERHALTEN EXAKT ERHALTEN (portiert, nicht neu erfunden): row None -> ('', True); Skalar None -> ('', True); OperationalError -> (None, False) + Warnung; Cache-Hit vor Query-Def vor SQL. NEU/Verbesserung: Flag write_cache. placeholders.py nutzt write_cache=True (Endpunkt fuellt placeholder_cache wie bisher, Bauplan B6 §3); report_source.py nutzt write_cache=False — der BERICHTS-EXPORT liest den Cache nur und SCHREIBT NICHT in evidence_<uid>.db. Damit ist der Export streng lese-only und respektiert den Migrationsvorbehalt sauberer als die gecachte Zwischenloesung aus Build 399. UMBAU: forensic_api/placeholders.py: _resolve_body und _refresh_cache delegieren an AutoQueryResolver; _execute_query ENTFERNT; ungenutzter Import sqlite3 entfernt; _PLACEHOLDER_RE und alle Endpunkte (handle_resolve/refresh/values/library) UNVERAENDERT. report_render/report_source.py: _resolve_auto ist nur noch ein Adapter auf self._auto.resolve_value_or_none; die frueher dort duplizierte Logik ist weg. TESTS: NEU tests/test_auto_query.py (AQ01-AQ08, 8 Tests) gegen eine ECHTE EvidenceDb (Cache) und eine ECHTE In-Memory-fdb (SQL) — kein MagicMock an der Schnittstelle: Cache-Hit, resolved, empty, no_query, sql_error, write_cache-Flag (schreibt/schreibt-nicht), resolve_value_or_none-Mapping, execute_query-Randfaelle. Die bestehenden Endpunkt-Tests tests/test_placeholders.py (T06-T10 resolve/refresh/library, T11 Regex) laufen UNVERAENDERT durch -> Beleg, dass das Endpunkt-Verhalten erhalten ist. REGRESSION lokal (Python 3.11): 1145 passed (Basis 1137 + 8), 49 skipped, 0 failures durch diese Aenderung; 1 Collection-Error bleibt tests/test_editor_renderer.py (Bestandscode editor/html_renderer.py:349, PEP-701-f-String erst ab 3.12 kompilierbar — auf VM-3.14 gueltig, kein Build-403-Bezug). OFFENE RESTPUNKTE (GR1, nicht still): (c) SQLite-Umfang — fdb-gespeiste Alt-Tabellen (profile_summary/known_aliases/network_summary/activity_stats/timeline_summary) sind NICHT im Bericht-Export; ob ein separater 'Fallakte-Gesamtexport' gewuenscht ist, bleibt offener mc-Punkt. (d) R2 inline-vs-Warnliste (aus 399) weiterhin offen zur mc-Bestaetigung. FOLGE-BUILD: 404 (PDF via reportlab; VORHER reportlab in prepare_deployment.py/RUNTIME_PACKAGES aufnehmen und VM-Wheels befuellen). MIGRATION: keine Schemaaenderung; report_render liest nur und schreibt jetzt auch keinen Cache mehr; placeholders.py-Endpunktverhalten unveraendert."
}

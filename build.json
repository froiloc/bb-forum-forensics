{
  "build": 497,
  "version": "0.8.497",
  "date": "2026-07-22",
  "note": "Build 497 (CASE-INSENSITIVITY der Validierung: neue Spalte templates.db.placeholders.validation_ci fuer regex/list/like). Basis origin/master 428f357 (v0.8.496). ENTHAELT EINE templates.db-MIGRATION -> vor Inbetriebnahme ausfuehren (siehe unten). templates.db wird nur gelesen (keine Ermittlerergebnisse) — trotzdem mit Backup/Idempotenz/integrity_check. JS kennt KEIN Inline-(?i); das 'i'-Flag wird am RegExp-Konstruktor gesetzt bzw. der Listenvergleich in Kleinschreibung gefuehrt (mc 2026-07-22). 0 = Gross-/Kleinschreibung beachten (bisheriges Verhalten, abwaertskompatibel), 1 = ignorieren; gilt fuer ALLE drei Pruefarten. MIGRATION (NEU management/migrate_templates_ci.py): idempotent, ALTER TABLE placeholders ADD COLUMN validation_ci INTEGER NOT NULL DEFAULT 0 (nicht-destruktiv, KEIN Tabellen-Neubau — anders als 489; Bestandszeilen erhalten 0 = exakt bisheriges Verhalten, Grundregel 1). Backup .pre497.bak (ausser --no-backup), Audit-Zeile (action 'migrate', target_type 'placeholder', falls templates_audit_log existiert), PRAGMA integrity_check. apply_migration(con) als reine Funktion (testbar). AUFRUF: python management/migrate_templates_ci.py --templates-db <pfad> ODER --config ./config.yaml. BACKEND: db/templates_db.py QueryRecord.validation_ci (int, Default 0); beide Reader-SELECTs (get_query/list_queries) lesen validation_ci; _row_to_query defensiv (fehlende Spalte -> 0, vor Migration). forensic_api/placeholders.py handle_library gibt validation_ci mit aus. management/templates_admin/placeholder_repo.py: _COLS + INSERT + UPDATE + _canon (Audit) um validation_ci erweitert (vci aus p, nur 0/1). management/server/management_app.py _placeholder_from_payload nimmt validation_ci in die Whitelist auf. INVESTIGATOR-FRONTEND: userinfo/validation_rules.js likeToRegExp(pattern, ci) + checkTyped(vtype, rule, wert, ci) — regex/like via 'i'-Flag, list via Kleinschreibungsvergleich; ohne ci-Argument case-sensitive (abwaertskompatibel). userinfo/placeholder_defs.js indiziert validation_ci (0/1). userinfo/placeholder_wizard.js _fieldCheck reicht def.validation_ci an checkTyped durch. MANAGEMENT-MASKE: management/server/static/cockpit_templates.js — Checkbox 'Gross-/Kleinschreibung ignorieren (regex/list/like)' im Validierungsblock; buildPayload sendet validation_ci (nur paarweise mit aktiver Validierung); testRule(vtype, rule, sample, ci) + likeToRegExp(pattern, ci) beruecksichtigen ci; _currentFields/_fillForm/_restoreDraft lesen/setzen die Checkbox; Live-Test (_refreshChecks) nutzt fCi.checked. management/server/static/cockpit.css: .aiw-tpl-cilabel. Semantik deckungsgleich Ermittler-Client / Management-Maske / (kuenftig) Server. TESTS: NEU tests/test_migrate_templates_ci.py (MC01-MC05: Spalte hinzugefuegt/Default 0, idempotent, Audit-Zeile, integrity ok, ohne placeholders-Tabelle RuntimeError). tests/test_templates_query_admin.py: Harness _mk_templates_db zieht die 497-Spalte nach (ADD_COLUMN_SQL, ohne Audit), NEU tp13 (validation_ci Round-Trip 1->0, a-Default 0). vitest: test_validation_rules_typed T13-T16 (likeToRegExp/checkTyped ci regex/list/like); test_placeholder_defs T02b (ci indiziert); test_placeholder_wizard T45 (Feldpruefung ci); test_cockpit_templates TT21 (testRule/likeToRegExp/buildPayload ci) + TT22 (ci-Checkbox -> Payload via DOM). REGRESSION: volle Python-Suite gruen unter Python 3.13 (1645 passed, 61 skipped, 6 subtests; +6); volle vitest-Suite gruen (1028 passed, 86 Dateien; +8); node --check + ast.parse OK; Routing-Waechter gruen. WICHTIG: In der VM zuerst die Migration ausfuehren, DANN testen (der Reader liest validation_ci; eine nicht migrierte templates.db liesse die Bibliothek leer laufen). ABGRENZUNG/OFFEN: Serverseitige DB-Validierung (checkTyped-Aequivalent inkl. ci) als zusaetzliche Zusicherung beim Einreichen ist noch nicht implementiert (bisher clientseitig + rule:-Katalog serverseitig) — bei Bedarf eigener Build. Persistenz der Klon-Markierung (Slice 3) wartet weiter auf deinen Entscheid (A/B/C)."
}

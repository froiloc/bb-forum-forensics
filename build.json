{
  "build": 437,
  "version": "0.7.437",
  "date": "2026-07-19",
  "note": "Build 437 (Wartungsmodus - Management-Integration, Build C der Sequenz; ADDITIV bis auf minimale, getestete Haken; KEINE Schemaaenderung, migrationsfrei). Der Management-Server nutzt ein PER-REQUEST-Verbindungsmodell (mode=ro, keine persistente Bundle-Verbindung) - daher ist Quiesce einfacher als beim Webserver: neue DB-Zugriffe blocken (503) + laufende austrudeln + ACK; Resume = nur Gate freigeben (nichts wieder aufzubauen). Gate/Controller/Poller aus Build 435/436 werden WIEDERVERWENDET. GEAENDERT: management/server/management_handler.py - maintenance_gate-Attribut (default None); Gate-Wrap in do_GET (nicht-SSE) und do_POST (do_POST-Rumpf nach _do_POST_impl ausgelagert, Wrapper haelt enter/leave); SSE (_handle_sse) prueft das Gate beim Eintritt (503) UND bricht die langlebige Verbindung ab, sobald eine Wartung beginnt (gate.is_blocked), damit der Drain durchkommt; _send_503_wartung (JSON 503 + Retry-After). management.py - import os; neues Argument --maintenance; Start-Guard (Start im Wartungsmodus NUR bei aktivem Fenster, sonst Verweigerung -> Schutz vor Missbrauch); Schritt 7 Integration (Gate am Server; ServerRegistration mit UUID bzw. PresenceBeacon [role='management']; Poller mit Callbacks: quiesce [block_and_drain -> ACK; kein Bundle-Close noetig; bei beenden server.shutdown], resume [ACK entfernen + Gate freigeben], Versions-Waechter, Selbstbeendigung, Kill); serve_forever-finally stoppt Poller und entfernt Anmeldung/Beacon/ACK. own_build via core.build_info.BuildInfo. GRENZE wie beim Webserver dokumentiert: offene SSE trudelt erst beim naechsten Poll-Intervall aus; block_and_drain laeuft sonst in den Timeout (30s) und faehrt mit Warnung fort. TESTS: tests/test_management_maintenance_gate.py, 3 Faelle mit ECHTEM Mini-Server (localhost, Fake-App): Gate offen -> GET+POST 200; Gate blockiert -> GET+POST 503 (503 vor Token-Pruefung, da Gate zuerst); Resume stellt Betrieb wieder her. Belegt zugleich, dass der do_POST-Umbau den Pfad nicht beschaedigt. Regression: GESAMTE pytest-Suite 1323 passed / 54 skipped, py_compile ueber gesamten Baum fehlerfrei, Import von management/handler ok. NAECHSTER SCHRITT: Build D (CLI tools/maintenance.py [enter/exit/status mit ACK-Wartung + Exklusiv-Lock-Beweis] und tools/maintenance_kill.py [list/kill])."
}

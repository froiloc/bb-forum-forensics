{
  "build": 466,
  "version": "0.7.466",
  "date": "2026-07-20",
  "note": "Build 466 (AP-2G Betrieb/Governance, LKAe-Distribution — Demo-/Mockup-Paket, NICHT PROD; mc 2026-07-20; MD5-Handshake bestaetigt). Idee 27, letztes AP-2G-Stueck. OFFLINE-PACKAGING-WERKZEUG (kein Cockpit-Feature): KEINE Migration, KEIN neues RBAC-Recht, KEIN Endpunkt — einzige geaenderte Bestandsdatei: build.json (Muster prepare_deployment.py). NEU management/distribution/demo_seed.py: baut eine frische, REIN SYNTHETISCHE coordinator.db ueber den echten Migration-Runner (M001..M017) + die echten auditierten Repos -> gueltige hash-verkettete Audit-Kette (Integritaets-Sicht im Demo gruen). Inhalt rein fiktiv (Demo-Personen, demo_boarderNN-Faelle, je ein Beispiel Promotion/Fallfreigabe/Onboarding/Extern-Vorgang); kein realer Fallinhalt -> Unbedenklichkeit (Fallregel 3) trivial erfuellt. NEU management/distribution/lkae_dist.py: build() baut ein self-contained Paket in ein SEPARATES Ziel (Whitelist-Code management/core/db/management.py/requirements.txt OHNE *.md/*.pyc/__pycache__ + Demo-config.yaml + start.sh/.bat + README_DEMO.md + Dockerfile + synthetische Demo-DB) und schreibt manifest.json mit SHA-256 je Datei + Freigabe-Vermerk + Manifest-Digest (Muster export/staging.py); verify() rechnet jede Datei nach (erkennt Aenderung/Fehlen/Ergaenzung). SCHUTZ (default-deny, GR1): ohne --freigabe kein Bau (die 'abhaengig: Freigabe' der Idee); Abbruch bei Ueberlappung mit einer PROD-Datenablage (NICHT PROD) und bei nicht-leerem Ziel; interne Planungs-*.md werden nie mitgepackt. NEU management/distribution/lkae_admin.py: CLI (build/verify; PROD-Pfad-Schutz aus config.yaml). TESTS: tests/test_demo_seed.py (DS01-04: valide Kette, AP-2G-Artefakte, rein synthetisch, kein Ueberschreiben), tests/test_lkae_distribution.py (LD01-07: Paketbau+Manifest+verify, Freigabe-Pflicht, PROD-Ueberlappung, nicht-leeres Ziel, Manipulations-/Zusatzdatei-Erkennung, Whitelist/kein-Docker). REGRESSION: py_compile OK; volle pytest 1545 passed/49 skipped; volle vitest 890 passed/1 skip/1 todo (unveraendert). LIVE-VERIFIKATION IN DER VM EMPFOHLEN (python -m management.distribution.lkae_admin build --target <dir> --freigabe \"...\"; danach verify). Damit ist AP-2G abgeschlossen. Buildnummer ggf. manuell anpassen, falls 466 parallel vergeben."
}

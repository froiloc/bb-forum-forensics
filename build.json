{
  "build": 485,
  "version": "0.8.485",
  "date": "2026-07-21",
  "note": "Build 485 (Chef-Freigabe W5: Dropdown-Header-Filter fuer Typ + Status, Entfernen des Status-<select> ueber der Tabelle; analog Lektorat Build 484; Frontend-only, rein lesend; mc-Wunsch). Setzt auf origin/master HEAD 5d227af (v0.8.483) auf (cockpit_approval.js Basis-MD5 8e6347302f928be941fa0eecd315ba57 aus Build 483). AENDERUNGEN management/server/static/cockpit_approval.js — deckungsgleich zu 484: (1) Der separate Status-<select> UEBER der Tabelle entfaellt vollstaendig (inkl. statusCounts + API-Export). (2) Statusfilterung ueber DROPDOWN-Header-Filter (Tabulator headerFilter 'list') der Status-Spalte mit 'Zur Abnahme vorgelegt'(submitted)/'Freigegeben'(approved)/'Versandt'(final)/'Entwurf'(draft) + 'alle' (leerer Wert = kein Filter); Default 'Zur Abnahme vorgelegt' via initialHeaderFilter; die Tabelle laedt ALLE Zeilen (toRows(..., 'alle')). (3) Status-Spalte ZEIGT status_label, FILTERT ueber den Roh-Status (neue reine Funktion _statusHeaderFilter -> rowData.status), damit 'Versandt' waehlbar bleibt. (4) Typ-Spalte als DROPDOWN mit 'Vermerk'/'Ergaenzungsvermerk'/'Abschlussbericht' + 'alle'. Benutzer/Titel/Verfasser bleiben Freitext, Paginierung 20/Seite bleibt. Die Auswahl (rowClick -> _selectReport -> _buildActionPanel/Belege/Kommentare/Ergebnis) und der SSE-Schutz (hasSelection, Build 479) bleiben unveraendert. API: statusCounts entfernt, _statusHeaderFilter als 'statusFilter' exportiert. GEAENDERT tests/unit/test_cockpit_approval.test.js: AP03 prueft ALLE Zeilen geladen + Typ/Status als list-Filter + initialHeaderFilter='submitted'; AP07 waehlt jetzt die freigegebene Zeile (Index 1); AP17 ersetzt (kein Select mehr; prueft _statusHeaderFilter inkl. 'alle'); AP18 entfernt. cockpit.css/cockpit.js NICHT geaendert (ungenutzte Regeln .aiw-approval-toolbar/.aiw-approval-status bleiben harmlos). Kein Server-/DB-/Schema-/Migrationseingriff, migrationsneutral. REGRESSION: volle vitest-Suite gruen; node --check cockpit_approval.js OK. Keine .py/.php-Aenderung. HINWEIS: Der Stub-Tabulator prueft die Konfig + die reine Filterfunktion; die tatsaechliche Wirkung von initialHeaderFilter/list im Browser bitte kurz sichtpruefen. Damit sind Lektorat (484) und Chef-Freigabe (485) auf dieselben Dropdown-Filter umgestellt."
}

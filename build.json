{
  "build": 398,
  "version": "0.7.398",
  "date": "2026-07-14",
  "note": "Build 398 (Baustelle 3/6 / PM-Uebersetzung: get_meta waehlt die Quelltabelle nach source). KONTEXT: Die PM-Uebersetzung wird vorbereitet (Bauplan PM-Uebersetzung Option B). BEFUND im aiw_translator (belegt, SQLite-Repro 2026-07-14): pm_extractor.py schrieb PMs in dieselbe Tabelle posts_cleaned, in der die Forenposts liegen - mit post_id als ALLEINIGEM PK und INSERT OR REPLACE. Forenpost- und PM-IDs stammen aus EIGENEN, ueberlappenden ID-Raeumen (pms_new_posts.id / pms_my_posts.post_id), daher haette eine PM den gleichnamigen Forenpost STILL ueberschrieben. Ein Flag als SPALTE (forum_id=-1 bzw. from_forum) verhindert das NICHT - die Quelle muss Teil des SCHLUESSELS sein. Gleiches Problem in translations (post_id allein PK). Verstoss gegen Grundregel 1. Noch nichts zerstoert: pm_extractor lief nie. LOESUNG (Option B): PM-Originale kommen in eine EIGENE Tabelle pms_cleaned (die grosse posts_cleaned mit 842k Zeilen / 6,5 GB bleibt unangetastet - kein Rebuild, kein Plattenplatz-Problem); translations bleibt EINE Tabelle (der Webserver liest genau sie), bekommt aber den zusammengesetzten PK (post_id, source). WEBSERVER-AENDERUNG (dieser Build): db/translations_db.py get_meta(post_id, source) waehlt die QUELLTABELLE nach source - 'posts' -> trdb.posts_cleaned, 'pms' -> trdb.pms_cleaned. Der LEFT JOIN auf trdb.translations filtert weiterhin auf source (dort ist die Quelle jetzt Teil des Schluessels). Unbekannte source -> None + WARNING (kein stiller Fallback). Fehlt pms_cleaned (PM-Extraktion noch nicht gelaufen), liefert get_meta sauber None -> Endpoint antwortet found:false; KEIN Absturz, aber IMMER protokolliert (GR1). list_translated_post_ids/get_translation bleiben unveraendert (filtern bereits auf source, Build 331). Der Endpoint /_forensic/translation_meta nimmt source bereits entgegen (Build 341) - keine Endpoint-Aenderung noetig. TESTS: tests/test_translation_meta.py um 3 Faelle erweitert (Post und PM mit IDENTISCHER post_id 705985 werden korrekt getrennt geliefert; source='pms' ohne pms_cleaned -> None, posts funktioniert weiter; unbekannte source -> None). REGRESSION run_tests.py: Python 1124 passed / 59 skipped / 6 subtests, JavaScript 689 passed / 1 skipped / 1 todo. DEPLOY: db/translations_db.py neu ausliefern + Server-Neustart. Der Build ist VORWAERTS- UND RUECKWAERTSKOMPATIBEL: er laeuft mit und ohne pms_cleaned. AUSSERHALB DIESES REPOS geliefert (aiw_translator, /opt/aiw_translator): pm_extractor.py (fehlender import sqlite3 - der gemeldete NameError - plus Schreibziel pms_cleaned statt posts_cleaned), orchestrator_api.py (translations mit PK (post_id, source); homogene Batches je Quelle, weil die Worker-Antwort nur post_id zurueckliefert und die Zuordnung bei gemischten Batches mehrdeutig waere; save_translation/mark_job_status/get_translation/get_translations_batch tragen source; get_stats zaehlt PMs mit), migrate_translations_source_pk.py (schema-introspizierende, idempotente, verifizierende Migration der bestehenden translations.db auf PK (post_id, source); mappt from_forum -> source und meldet Widersprueche statt sie zu glaetten)."
}

{
  "build": 421,
  "version": "0.7.421",
  "date": "2026-07-14",
  "note": "Build 421 (Baustelle 7, Vermaehlung Berichtseditor B6 x Management B7, Welle 1) - SF-4, TEIL 2: AUDITIERTER templates.db-SCHREIBPFAD (TemplatesWriter) + Audit-CHECK-Erweiterung. Autoritativ: mc 2026-07-14. Basis Build 420 (dfcca1d); MD5-Handshake bestanden. PYTHON/BACKEND - kein JS geaendert. ZWECK: Fundament, ueber das die Authoring-Werkzeuge W1/W2/W3 kuenftig AUSSCHLIESSLICH schreiben (TemplatesDb bleibt read-only). NEU (Grundregel 10): management/gateway/templates_writer.py - TemplatesWriter (analog CoordinatorWriter): der EINZIGE zulaessige Schreibpfad auf templates.db. audited_write(do_write, action, target_type, changed_by) bindet den fachlichen Write UND den templates_audit_log-Eintrag in EINE Transaktion (BEGIN IMMEDIATE; COMMIT nur gemeinsam, sonst ROLLBACK). do_write liefert ein dict mit 'target_id' (+ optional old_value/new_value); fehlt 'target_id' -> TemplatesWriteError + Rollback (kein stiller Teilschreib). isolation_level=None (explizite Tx), journal_mode=delete beim Aufrufer (Build 408/409, kein WAL). NEU: management/migrate_templates_audit_check.py - standalone, idempotentes Migrationsskript (templates.db hat keinen Runner): erweitert templates_audit_log.target_type-CHECK von ('module','query') auf ('module','query','template') per VERLUSTFREIEM Tabellen-Rebuild (neue Tabelle mit erweiterter CHECK, Zeilen kopieren, alte droppen, umbenennen) in einer Transaktion. Idempotenz-Check ueber den quotierten Wert 'template' im CHECK-DDL; Inline-Verifikation nach dem Rebuild. Aufruf: python -m management.migrate_templates_audit_check [--templates-db PATH]. MIGRATION/01.07.2026: templates.db ist aenderbar (Ergebnis-verlustfrei; die Ermittler speichern dort keine Ergebnisse) - der Rebuild ist verlustfrei (bestehende Audit-Zeilen bleiben erhalten, per Test belegt). KEINE Aenderung an evidence_/forensic_/assets_/coordinator_. TESTS: +6 pytest (tests/test_templates_writer.py, TW01-TW06 gegen eine echte temporaere templates.db): TW01 Migration erweitert CHECK + bestehende Zeile ueberlebt + idempotent; TW02 'template'-Audit vor der Migration scheitert an der CHECK, danach zulaessig; TW03 audited_write schreibt Modul + Audit atomar; TW04 audited_write target_type='template' nach der Migration; TW05 Rollback bei Fehler in do_write (weder Write noch Audit); TW06 fehlendes 'target_id' -> TemplatesWriteError + Rollback. REGRESSION (Cloud, Python 3.11): pytest 1221 passed / 49 skipped / 6 subtests (inkl. der 6 neuen); vitest UNVERAENDERT (kein JS) -> 741 passed aus 419 gilt fort; tests/test_editor_renderer.py bleibt das bekannte Py-3.11-Artefakt (VM-3.14 gruen), fuer den Cloud-Lauf ausgeklammert. py_compile OK. VM (Python 3.14): 'python run_tests.py' erwartet gruen; danach EINMALIG 'python -m management.migrate_templates_audit_check' auf der eingesetzten templates.db ausfuehren (damit W3-Vorlagen-Audits mit target_type='template' moeglich werden). MD5-VORBEHALT: Vor dem produktiven Lauf des Migrationsskripts bitte die MD5 der eingesetzten templates.db bestaetigen (Grundregel 8). SF-4 (TemplatesWriter-Fundament) damit KOMPLETT. NAECHSTER SCHRITT: W2 (Platzhalter+Query-Editor) - erstes Authoring-Werkzeug auf dem TemplatesWriter, mit fdb-Dry-Run der Query (engster Validator); danach W3 (Dokumentvorlagen) und W1 (Baustein-Module)."
}

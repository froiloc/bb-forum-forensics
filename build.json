{
  "build": 362,
  "version": "0.7.362",
  "date": "2026-07-10",
  "note": "Build 362 (Baustelle 7, Welle 1): POLICY-SICHT TEIL 2 - Cockpit-Sicht (Frontend). Frontend-Teil des Split (Backend /api/policy = 361). Reiner Frontend-Build (kein Backend/keine Migration). Autoritativ: Bauplan_Baustelle7_Management_v1_1 Paragraph 11.3; management/Bauplan_Build362_PolicyView_v0_1.md; mc 2026-07-10 (Policy-Sicht; Split 361/362). DEPLOY-HINWEIS: cockpit.html geaendert -> .gitignore '*.html' -> 'git add -f management/server/static/cockpit.html'. (1) NEU management/server/static/cockpit_policy.js (IIFE, Live-DEBUG, UMD -> vitest; window.AIWCockpitPolicy): rein capLabelIndex (cap-code -> label), grantRows (grants -> Zeilen, angereichert um Faehigkeits-Label), assignmentRows (assignments -> Zeilen), scopeText; DOM renderPolicy(mainEl, data, {Tabulator}) rendert Kopf + counts, zwei Tabulator-Tabellen GRANTS (Rolle/Faehigkeit/Bezeichnung/Scope/Beleg/Notiz, mit headerFilter) und ROLLEN-ZUWEISUNGEN (Person/Kennung/Rolle/Beleg) + Rollen-/Faehigkeiten-Katalog als kompakte Referenzliste; Tabulator injizierbar; gibt Array der Instanzen zurueck. XSS: nur textContent / Tabulator-plaintext (kein innerHTML). (2) GEAENDERT management/server/static/cockpit.js: state.tables (Array mehrerer Tabulator-Instanzen) + cleanupView baut sie ab; loadPolicy holt /api/policy und rendert via AIWCockpitPolicy (state.tables); selectView 'policy'-Zweig; SSE 'changed' laedt aktive Policy-Sicht neu. (policy war bereits im VIEW_CATALOG -> kein Nav-Count-Change.) (3) GEAENDERT management/server/static/cockpit.html: cockpit_policy.js (defer) eingebunden. (4) NEU tests/unit/test_cockpit_policy.test.js (PO01-PO05, JSDOM): API, capLabelIndex+grantRows (Label-Anreicherung), assignmentRows, renderPolicy (zwei Stub-Tabulator-Instanzen, Kopf/counts, Katalog), ohne Tabulator -> Platzhalter+[] (Katalog dennoch da). REGRESSION run_tests.py: Python 947 passed (unveraendert - reines Frontend) / 59 skipped / 3 subtests; JavaScript 524 passed (519+5) / 1 skipped / 1 todo (526), 42 Testdateien (41+1). BROWSER-ABNAHME (console-first): policy.view ist fuer supervisor gegrantet -> Cockpit laden -> Tab 'Rechte / Policy' zeigt die Grants-Tabelle (Rolle->Faehigkeit), die Zuweisungs-Tabelle (Person->Rolle) und den Katalog - genau die Matrix, die zuvor per SQL gelesen wurde. Tabellen sind sortier-/filterbar. SSE-Reload ohne F5. Bei Auffaelligkeiten window.AIW_COCKPIT_DEBUG=true -> Console -> PoC -> Fix. STAND: Policy-Sicht komplett (361 Backend, 362 Frontend). Welle-1 verdrahtete Sichten bisher: Dashboard, Integritaet, Lastverteilung, Kapazitaet, Rechte/Policy. Weiterhin offen: Zuweisung (Schreib-Sicht), Ermittler-Betreuung, Berichts-Abnahme, Statistiken, Support-Historie, Meine Auftraege, Meine Historie. HINWEIS: Bauplan-.md + cockpit.html per .gitignore -> 'git add -f'. GELIEFERT: siehe ZIP (per-file MD5)."
}

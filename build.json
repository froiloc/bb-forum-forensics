{
  "build": 349,
  "version": "0.7.349",
  "date": "2026-07-10",
  "note": "Build 349 (Baustelle 7, Welle 1): INTEGRITAETS-/OPS-SICHT + globaler Ketten-Banner + LIVE-DEBUG. Reiner Frontend-Build (kein Backend/keine Migration); baut auf 348 auf. Endpunkt /api/integrity existiert seit 346 ({ok, first_bad_seq, detail, tip_seq}). Autoritativ: Bauplan_Baustelle7_Management_v1_1 Paragraph 11.1/11.2; management/Bauplan_Build349_Integritaet_v0_1.md; mc 2026-07-10 (Entscheidungen: Banner global sofern ops.view; ohne ops.view still; Live-DEBUG in allen drei Modulen). WICHTIG/DEPLOY-FIX: management/server/static/cockpit.html fehlte im gepushten Repo, da .gitignore-Regel '*.html' (Zeile 39) sie bei 347/348 vom Commit ausschloss (lokal vorhanden -> Browser lief; Frisch-Deploy haette auf '/' ein 404 geliefert, da ManagementApp._index sie ausliefert). cockpit.html ist in diesem ZIP im 349-Stand enthalten und MUSS mit 'git add -f management/server/static/cockpit.html' eingecheckt werden. (1) NEU management/server/static/cockpit_integrity.js (IIFE, Live-DEBUG, reine Funktionen UMD-exportiert -> vitest; window.AIWCockpitIntegrity): rein bannerModel(data) -> {klass ok/fehler, text} ('Kette intakt bis Sequenz N' bzw. 'KETTENBRUCH ab Sequenz M'; fehlende Sequenzen werden nicht still verschluckt -> '?'); DOM applyBanner(el, model) (Klasse+Text, entfernt aiw-integrity-hidden) und renderIntegrity(main, data) (Karte: Status mit Ampelpunkt, tip_seq, first_bad_seq, detail). XSS-sicher (detail via textContent). (2) GEAENDERT management/server/static/cockpit.js: selectView 'integrity'-Zweig -> loadIntegrity(); loadIntegrity holt /api/integrity, rendert die Sicht UND aktualisiert den Banner (eine Quelle: applyIntegrity); refreshBanner() haelt den Banner GLOBAL frisch (sofern ops.view) und schaltet ihn ohne ops.view still (aiw-integrity-hidden, kein 403-Rauschen); SSE 'changed' laedt aktive Sicht neu (dashboard->Overview, integrity->Integritaet) UND zieht den Banner nach; boot() ersetzt den 348-Platzhaltertext durch refreshBanner() (Doppel-Fetch vermieden, wenn 'integrity' erste Sicht). LIVE-DEBUG: debugOn() liest window.AIW_COCKPIT_DEBUG bei JEDEM log()-Aufruf (zur Laufzeit umschaltbar, kein Reload). (3) GEAENDERT management/server/static/cockpit_overview.js: Live-DEBUG (debugOn). (4) GEAENDERT management/server/static/cockpit.html: cockpit_integrity.js vor cockpit.js eingebunden; Banner-Kommentar auf 349 aktualisiert. (5) GEAENDERT management/server/static/cockpit.css: .aiw-integrity-hidden (display:none) fuer den stillen Banner ohne ops.view. (6) NEU tests/unit/test_cockpit_integrity.test.js (IN01-IN08, JSDOM): bannerModel ok/!ok/fehlende-Sequenz, applyBanner (Klasse/Text/hidden entfernt), renderIntegrity ok/!ok (Ampelpunkt, tip_seq, first_bad_seq), detail via textContent (XSS). REGRESSION run_tests.py: Python 878 passed / 59 skipped / 3 subtests (unveraendert ggue. committetem 348 - reines Frontend); JavaScript 504 passed (+8 Integritaet) / 1 skipped / 1 todo (506), 39 Testdateien. BROWSER-ABNAHME: Als supervisor (dashboard.view+ops.view) Cockpit laden -> gruener Ketten-Banner 'Kette intakt bis Sequenz N'; Tab 'Integritaet / Betrieb' zeigt die Karte. SSE: auditierte Aenderung (z.B. rbac_admin grant) -> Banner-Sequenz steigt ohne F5. Live-DEBUG: window.AIW_COCKPIT_DEBUG=true in der Console wirkt sofort (kein Reload). HINWEIS: Bauplan-.md per .gitignore untracked -> 'git add -f'. GELIEFERT: siehe ZIP (per-file MD5)."
}

{
  "build": 467,
  "version": "0.7.467",
  "date": "2026-07-20",
  "note": "Build 467 (AP-2E, Audit-/Revisions-Explorer, Backend+Frontend kombiniert; mc 2026-07-20; MD5-Handshake bestaetigt). Idee 24, REIN LESEND (audit_log ist per Trigger append-only) — KEINE Migration, KEIN neues RBAC-Recht (Reuse ops.view wie die Integritaets-Sicht), daher rbac_schema/dashboard-Tests unveraendert. NEU management/audit/audit_explorer.py: read-only AuditExplorer.query() (Filter event_type[mehrfach]/actor_id/target_type/target_id/seq-Bereich/ts-Bereich, Paginierung mit total/limit/offset/has_more, Akteursname per LEFT JOIN person, ausschliesslich parametrisiert), facets() (vorhandene Event-Typen + Akteure), get(seq); limit-Clamp 1..500. NEU management/audit/audit_export.py: reine Render-Funktion render_html() -> gerichtsfestes, self-contained HTML der gefilterten Treffer, umschlossen vom AP-2B-ExportEnvelope (Aktenkopf + Klassifikation + Erzeugungsvermerk mit Ersteller/Build/verify_chain-Ergebnis+Ketten-Spitze + SHA-256-Pruefsumme des Nutzinhalts), XSS-sicher via html.escape, UTC-Zeitstempel. GEAENDERT management/server/management_app.py: GET /api/audit (gefilterte Seite), GET /api/audit/facets, GET /api/audit/export (text/html, envelope-geprueft; Export-Obergrenze 2000 Zeilen -> 'Auszug'-Vermerk bei mehr) — alle unter ops.view; das Ansehen wird NICHT auditiert (reiner Lesezugriff, wie Integritaets-/Policy-Sicht). NEU management/server/static/cockpit_audit.js: Sicht 'audit' (Gruppe Administration, ops.view) mit Filterleiste (Event-Typ/Akteur aus Facetten, Ziel-Typ, seq-Bereich), paginierter Tabelle (seq/Zeit-UTC/Akteur/Ereignis/Ziel/Payload-Kurzform), Blaettern und Link 'Gerichtsfester Export' (traegt die angewandten Filter); IIFE, DEV-Logging, XSS-sicher, UMD-Ausgang. GEAENDERT cockpit.js (VIEW_CATALOG 'audit' nach 'integrity'; loadAudit mit Filter/Offset-State; Dispatch + SSE-Reload), cockpit.html (<script>), cockpit.css (.aiw-audit-*). TESTS: test_audit_explorer.py (AE01-07), test_audit_export.py (AX01-04), tests/unit/test_cockpit_audit.test.js (AU01-07); GEAENDERT tests/unit/test_cockpit_nav.test.js (VIEW_CATALOG 26->27, ops.view zeigt nun integrity+audit+promotion). REGRESSION: py_compile/node --check OK; volle pytest 1556 passed/49 skipped; volle vitest 897 passed/1 skip/1 todo. LIVE-VERIFIKATION IN DER VM EMPFOHLEN (Filtern/Blaettern + Export im echten Browser). HINWEIS: keine neuen Grants noetig (ops.view). Damit ist AP-2E abgeschlossen. Buildnummer ggf. manuell anpassen, falls 467 parallel vergeben."
}

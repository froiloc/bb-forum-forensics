{
  "build": 459,
  "version": "0.7.459",
  "date": "2026-07-19",
  "note": "Build 459 (AP-2G Betrieb/Governance, Baustein 6/n - Kommandopalette Fall-Sprung, Frontend; mc 2026-07-19; MD5-Handshake bestaetigt). Vervollstaendigt den Paletten-Fallsprung: die Palette sucht jetzt zusaetzlich FAELLE (ueber /api/search, Build 458) und springt beim Waehlen eines Falls zur Uebersicht mit fokussierter Zeile. REINES FRONTEND; kein Endpunkt/DB/Migration. GEAENDERT management/server/static/cockpit_palette.js: init(+searchCases, +onSelectCase); render() zeigt Sichten SOFORT (synchron, rechte-gefiltert) und haengt bei nicht-leerem Begriff asynchron FALL-Treffer an (nur wenn die Antwort noch aktuell ist - _searchToken verwirft out-of-order-Antworten); vereinheitlichtes Listenmodell (view/case-Items) fuer Tastatur-Navigation; typ-bewusste Auswahl (choose -> onSelect bzw. onSelectCase); Fall-Items tragen data-case-id, XSS-sicher via textContent. GEAENDERT management/server/static/cockpit_overview.js: Tabulator index='user_id'; NEU focusCase(table, userId) - scrollToRow + kurze Inline-Hervorhebung der Zeile, voll abgesichert (fehlt Zeile/Tabelle -> false, GR1), in API exportiert. GEAENDERT management/server/static/cockpit.js: Palette-init um searchCases (fetch /api/search; 403/Fehler -> leere Liste, Palette bleibt nutzbar) + onSelectCase (state.focusCaseId setzen, selectView('dashboard')); loadOverview fokussiert nach dem Rendern die gewaehlte Zeile und setzt focusCaseId zurueck. Bestehende Paletten-/Nav-Tests unveraendert (Init ohne searchCases -> nur Sicht-Suche, rueckwaertskompatibel); kein VIEW_CATALOG-Eintrag. TESTS: tests/unit/test_cockpit_palette_cases.test.js (PC01-PC04: Fall-Treffer erscheinen, Auswahl ruft onSelectCase+schliesst, veraltete Antwort verworfen, ohne searchCases nur Sichten), tests/unit/test_cockpit_overview_focus.test.js (OF01-OF03: scrollToRow+Hervorhebung+true, unbekannte Zeile->false, fehlende Tabelle/userId->false). REGRESSION: node --check OK; volle pytest + vitest gruen (siehe Handshake). LIVE-VERIFIKATION IN DER VM EMPFOHLEN (Fall-Suche + Sprung/Fokus im echten Browser). Damit ist der Paletten-Fallsprung vollstaendig (458 Endpunkt + 459 Frontend). HINWEIS: Buildnummer ggf. manuell anpassen, falls 459 parallel vergeben."
}

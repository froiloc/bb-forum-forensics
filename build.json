{
  "build": 354,
  "version": "0.7.354",
  "date": "2026-07-10",
  "note": "Build 354 (Baustelle 7, Welle 0): BACKUP TEIL 3 - REGISTRY + AUDIT + CLI (Welle-0-Backup ABGESCHLOSSEN). Additive Migration m007 auf coordinator.db. Autoritativ: Bauplan_Baustelle7_Management_v1_1 Paragraph 11 (backups-Registry, BACKUP_CREATED); m005-Prinzip; management/Bauplan_Build354_BackupRegistry_v0_1.md; mc 2026-07-10 (Zeile pro Datei; EIN Beleg pro Lauf; CLI mit dabei). (1) GEAENDERT management/audit/event_types.py: EventType.BACKUP_CREATED='backup_created' AKTIVIERT (aus Reserve) und in ALL aufgenommen. Append-only Vokabular: nichts umbenannt/entfernt. (2) NEU management/migrations/coordinator/m007_backups.py (VERSION=7, NAME=..., KIND='additive'): Tabelle 'backups' (EINE Zeile pro DB pro Lauf) mit Spalten run_ts/host/db_label/src_path/backup_path/sha512/size/user_version/integrity_ok/error/manifest_path/audit_seq NOT NULL->audit_log(seq)/created_at + Index ix_backups_label_ts(db_label, run_ts). Idempotent (CREATE ... IF NOT EXISTS + Guard mit INFO-No-op), Inline-Verifikation (raise->Rollback). Startet LEER (Zeilen nur ueber auditierten Schreibpfad). (3) NEU management/backup/backups_repo.py: BackupsRepo.record_run(run, actor_id) schreibt EINEN BACKUP_CREATED-Beleg (Lauf-Zusammenfassung als Payload) und je DB eine 'backups'-Zeile mit dessen audit_seq (after_audit-Hook des CoordinatorWriter -> Write+Audit+Registry atomar). Auch fehlgeschlagene DBs bekommen eine Zeile (integrity_ok=0, error=<grund>) - Fehlversuch bleibt belegt. list_backups() liest (optional je db_label). (4) GEAENDERT management/backup/backup_executor.py: BackupRun um run_ts/host erweitert (fuer die Registrierung). (5) NEU management/backup/backup_admin.py: CLI 'plan' (Trockenlauf, kein Write), 'run' (verweigert bei fehlgeschlagener Vorabpruefung; sichert; registriert auditiert; Zusammenfassung + audit_seq), 'list' (Registry). Pfade/Rahmen aus config.yaml (paths.*/backup.*), --coordinator-db override; main(argv) testbar. (6) NEU tests/test_backups_registry.py (BR01-BR04, CL01-CL02): m007 Tabelle/Index + idempotent, BACKUP_CREATED-Vokabular, record_run koppelt je DB-Zeile an EINEN Beleg-seq, list_backups Filter, CLI run->list End-to-End (Backups im Ziel + Registry-Zeilen), CLI plan schreibt nichts. (7) GEAENDERT tests/test_management_dashboard.py: d01 Migrationsliste um 7 (m007) erweitert (erwartetes Pinning nach additiver Migration). REGRESSION run_tests.py: Python 905 passed (899+6) / 59 skipped / 3 subtests; JavaScript 511 passed (unveraendert) / 1 skipped / 1 todo (513), 40 Testdateien. MIGRATION/DEPLOY: m007 ist additiv (kein Datenverlust); wird beim naechsten Migrationslauf automatisch angewandt (MIGRATION_APPLIED in der Hash-Kette). CLI-Nutzung: python -m management.backup.backup_admin {plan|run|list} [--config ./config.yaml] [--actor h0a2898]. WELLE-0-BACKUP KOMPLETT (Config/Planner/Platzpruefung 352, Executor 353, Registry/Audit/CLI 354). HINWEIS: nur die Bauplan-.md ist per .gitignore untracked -> 'git add -f'. GELIEFERT: siehe ZIP (per-file MD5)."
}

{
  "build": 280,
  "version": "0.6.280",
  "date": "2026-06-07",
  "note": "Build 280: Bug 2.23 vollstaendig behoben. _loadReportImpl ruft jetzt reportLayer.open(report.id) auf — der ReportLayer ist die einzige autorisierte Quelle fuer Block-Daten, der separate fetch() auf /_forensic/report?format=json entfaellt. Zusaetzlich Backend-Bug in _action_open_report gefixt: o.block_id → o['block_id'] (get_block_order_for_report gibt list[dict] zurueck, kein Objekt mit Attributen)."
}

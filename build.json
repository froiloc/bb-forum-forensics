{
  "build": 453,
  "version": "0.7.453",
  "date": "2026-07-19",
  "note": "Build 453 (AP-2F Steuerung/Eskalation, Baustein 3/3 - Eskalationsregeln; mc 2026-07-19). Wertet den Fallzustand gegen TRANSPARENTE, konfigurierbare Regeln aus und erhebt belegte Eskalationen fuer die Chef-Ermittlerin. REIN LESENDE Auswertung/Anzeige; das auditierte BESTAETIGEN/Handeln einer Eskalation ist bewusst ein spaeterer, schreibender Schritt (F3) - hier NICHT enthalten. Additiv; kein Migrationsvorbehalt. NEU management/cases/escalation.py: EscalationThresholds (red_overdue_days=30, stale_open_days=14, backlog_high=10; via config.yaml escalation.* uebersteuerbar). evaluate_escalations(overviews, thresholds, now) = REINE Regel-Auswertung (dateilos testbar) mit drei Regeln: R1 fall_ueberfaellig (offener Fall, Ampel rot UND >= red_overdue_days inaktiv -> hoch), R2 fall_unbearbeitet (zugewiesen, Status open, >= stale_open_days inaktiv -> mittel; uebersprungen wenn R1 fuer den Fall greift -> KEINE Doppelmeldung), R3 rueckstau_hoch (unzugewiesener Rueckstau >= backlog_high -> hoch, systemisch user_id None). BELEGTREUE (GR1): jede Eskalation nennt die konkreten Signale (Tage inaktiv, Status, Zahl); keine Regel feuert ohne belegte Bedingung. Ordnung Severity (hoch>mittel>niedrig), dann meiste Inaktivitaet, dann uid; Zaehlungen count_hoch/mittel/niedrig. escalation_thresholds_from_config (None-sicher), escalation_to_dict. NEU management/cases/escalation_repo.py: EscalationRepo(con).compute() via DashboardRepo (read-only). NEU management/cases/escalation_admin.py: NUR-LESENDE CLI (--json). TESTS tests/test_management_escalation.py (ES01-ES08: R1 mit Beleg, R1 nicht unter Schwelle/gruen, R2 stale, keine Doppelmeldung, R3 systemisch, abgeschlossene keine Eskalation, Ordnung+Zaehlungen, config None-sicher + dict). REGRESSION: py_compile OK; volle pytest-Suite gruen (siehe Handshake). Damit ist AP-2F komplett (451 Ueberlastwarnung, 452 Naechstbeste-Aktion, 453 Eskalationsregeln). HINWEIS: Buildnummer ggf. manuell anpassen, falls 453 parallel vergeben."
}

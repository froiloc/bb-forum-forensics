{
  "build": 377,
  "version": "0.7.377",
  "date": "2026-07-10",
  "note": "Build 377 (Baustelle 7): BERICHTS-VERSIEGELUNG (Backend). Keine coordinator-Migration; NEUE zentrale DB approved_reports.db (wird bei Bedarf angelegt). Autoritativ: mc 2026-07-10 (Siegel an ZWEI Orten; Hash-Konvention aus core/startup_checks.py; OHNE report_comments); management/Bauplan_Build377_ReportSealing_v0_1.md. (1) NEU management/reports/report_sealer.py (ReportSealer): kanonischer INHALTSHASH + statisches Abbild eines Berichts aus evidence_<uid>.db (read-only). HASH-KONVENTION uebernommen aus core/startup_checks.py (kein neues Rad): NICHT ueber Datei-Bytes (bei SQLite instabil), sondern kanonischer Inhaltsdump '<tabelle>:<repr(col)>|...' als UTF-8 -> SHA-256. UMFANG (mc): reports (Kopf) + report_blocks IN DER ANZEIGE-REIHENFOLGE aus report_block_order + report_anchors. NICHT im Siegel: report_comments (Arbeitsmaterial) und report_approvals/report_opened (Metadaten der Abnahme). WICHTIG: 'status' ist AUS dem Hash ausgenommen - der Status aendert sich DURCH die Freigabe; waere er im Hash, wuerde das Siegel im Moment des Siegelns ungueltig (selbstreferenzieller Konflikt; dieselbe Begruendung wie der Ausschluss des sha256-Eintrags in forensic_meta). Bloecke ohne Order-Eintrag werden deterministisch ans Ende gehaengt statt verschwiegen (Grundregel 1). (2) NEU management/reports/approved_reports_db.py (ApprovedReportsDb): zentrale Siegel-DB approved_reports.db mit Tabelle approved_reports (user_id, report_id, content_sha256, snapshot_json = vollstaendiges statisches Abbild, Kopfdaten, approved_by/_id, approved_at, is_final, note, audit_seq -> Beleg in coordinator.audit_log; UNIQUE(user_id, report_id, content_sha256, is_final)). is_final gehoert in den UNIQUE-Schluessel: die Aufwertung approved->final siegelt denselben Inhalt erneut (der Inhalt aendert sich dabei ja gerade nicht) - legitimer zweiter Eintrag, kein Duplikat. (Vom Test aufgedeckt.) (3) NEU management/reports/approval_service.py (ApprovalService): approve() und verify(). DAS ZWEI-DATENBANKEN-PROBLEM wird OFFEN BENANNT statt versteckt: Beleg (coordinator.db), Bericht (evidence_<uid>.db) und Siegel (approved_reports.db) liegen in drei SQLite-Dateien - eine gemeinsame Transaktion gibt es NICHT, ein 'atomares' Versprechen waere gelogen. Reihenfolge: (1) lesen+hashen (read-only), (2) BELEG REPORT_APPROVED via CoordinatorWriter -> audit_seq, (3) SIEGEL zentral ablegen (Abbild + Hash + audit_seq), (4) DURCHSETZUNG in evidence (report_approvals + reports.status=approved/final; NUR bestehende Tabellen/Spalten -> KEINE Schemaaenderung, Migrationsvorbehalt gewahrt). Scheitert (3) oder (4), bleibt der Beleg stehen (der Versuch HAT stattgefunden) und der Aufrufer bekommt einen EXPLIZITEN Fehler mit der Angabe, was gelungen ist - ein stiller Teilerfolg ist ausgeschlossen (Grundregel 1). Vorbedingungen: Freigabe nur aus 'submitted'; 'final' nur aus 'approved'. (4) GEAENDERT management/audit/event_types.py: REPORT_APPROVED + REPORT_SEAL_VERIFIED (in ALL registriert). (5) GEAENDERT core/config_loader.py: paths.approved_reports_db (Default './data/approved_reports.db'). (6) GEAENDERT management/server/management_app.py: ManagementApp(..., approved_db=) injizierbar; POST /api/report/approve (reports.approve, Scope 'alle' - die Abnahme ist per Definition fremdbezogen; 409 mit Begruendung bei verletzter Vorbedingung/Teilfehler) und GET /api/report/verify (reports.review ODER reports.approve). Versionskopf -> 377. (7) NEU tests/test_report_sealing.py (SE01-SE07): Hash deterministisch UND status-unabhaengig UND kommentar-unabhaengig; Hash erkennt Inhaltsaenderung; Hash erkennt REIHENFOLGE-Aenderung (die Reihenfolge ist Teil des Berichts); Freigabe schreibt alle drei Orte (evidence-Status + report_approvals, zentrales Siegel mit Abbild, coordinator-Beleg); Vorbedingungen; SE06 MANIPULATIONSNACHWEIS (nach direkter Aenderung der evidence-DB am Werkzeug vorbei meldet verify() ABWEICHUNG); Endpunkte (403 ohne Cap/bei Scope 'eigene', Erfolg, verify, 409 bei erneuter Freigabe). REGRESSION run_tests.py: Python 994 passed (987+7) / 59 skipped / 3 subtests; JavaScript 565 passed (unveraendert - Backend-only) / 1 skipped / 1 todo (567), 49 Testdateien. ABNAHME: Server NEU STARTEN. Einen Bericht in einer evidence-DB auf status='submitted' setzen. Dann POST /api/report/approve {user_id, report_id} mit Content-Type: application/json und X-AIW-Token -> 200 mit content_sha256, audit_seq, seal_id. Pruefen: evidence.reports.status='approved'; approved_reports.db enthaelt Abbild+Hash; audit_log enthaelt report_approved. Danach GET /api/report/verify?user_id=..&report_id=.. -> match=true. Zum Gegenbeweis den Bericht direkt in der evidence-DB aendern -> verify meldet match=false und 'ABWEICHUNG'. NAECHSTER BUILD 378: Frontend der Freigabe (Knopf in der Berichts-Abnahme, Siegel-/Verify-Anzeige). Danach 379: SCHREIBSPERRE im Ermittler-Webserver bei Status approved/final (Durchsetzung dort, wo geschrieben wird). HINWEIS: nur die Bauplan-.md ist per .gitignore untracked -> 'git add -f'. GELIEFERT: siehe ZIP (per-file MD5)."
}

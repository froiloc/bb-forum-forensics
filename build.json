{
  "build": 471,
  "version": "0.8.471",
  "date": "2026-07-20",
  "note": "Build 471 (AP-2A(2b), Cockpit-Sicht Kreuzbezug, Frontend-only; mc 2026-07-20; MD5-Handshake bestaetigt). Frontend zu den Endpunkten aus Build 470 (GET /api/crossref, POST /api/crossref/set). subject_id-nativ (Basis nach M019). NEU management/server/static/cockpit_crossref.js: Modul nach Vorlage cockpit_onboarding.js (IIFE + use strict, DEV-Logging AIW_COCKPIT_DEBUG, ausfuehrliche Kommentare, reine Helfer ohne DOM, UMD-Export window.AIWCockpitCrossref/module.exports). Rendert den Katalog identifizierter Personen (Konto subject_id -> reale Person) mit Konfidenz-Badge (verdacht/wahrscheinlich/gesichert) und - mit Recht crossref.edit - ein Formular zum Anlegen/Revidieren samt Zeilen-Aktion Revidieren (befuellt das Formular vor). KEIN optimistisches UI (nach Schreiben Sicht neu laden). XSS-sicher (alle variablen Texte via textContent). Sensibilitaet: real_identity/basis/note bleiben serverseitig aus dem Audit-Payload; das Frontend reicht nur durch. GEAENDERT management/server/static/cockpit.js: VIEW_CATALOG-Eintrag {id crossref, cap crossref.view, group Auswertung, label Kreuzbezug}; loadCrossref() (fetchJson /api/crossref, postJson /api/crossref/set, canEdit via hasCap crossref.edit); View-Dispatch + SSE-Refresh-Dispatch fuer crossref. GEAENDERT management/server/static/cockpit.html: Skript cockpit_crossref.js eingebunden. GEAENDERT management/server/static/cockpit.css: Konfidenz-Badge-Stile (drei Stufen) + Kreuzbezug-Tabellen-/Formular-Stile (Basis-Variablen wiederverwendet). TESTS: NEU tests/unit/test_cockpit_crossref.test.js (CX01-CX07, gegen echten Code via JSDOM/UMD, keine Logik-Duplikation). GEAENDERT tests/unit/test_cockpit_nav.test.js (VIEW_CATALOG-Laenge 27 -> 28 + CN-XREF: crossref haengt an crossref.view/Gruppe Auswertung). REGRESSION: run_tests.py (Python + JavaScript) gruen; py_compile unveraendert (kein Python geaendert). HINWEIS: keine neue Migration/Faehigkeit (in 468/470 vorhanden). Grants fuer crossref.* bleiben operative mc-Entscheidung (default-deny) - ohne Grant erscheint die Sicht nicht in der Nav. Wiedervorlage-Eintrag zur zurueckgestellten Verwertbarkeits-Achse (StA) liegt diesem Build bei. Buildnummer ggf. manuell anpassen, falls 471 parallel vergeben."
}

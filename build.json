{
  "build": 376,
  "version": "0.7.376",
  "date": "2026-07-10",
  "note": "Build 376 (Baustelle 7, Betriebssicherheit): MIGRATIONSSTAND-PRUEFUNG beim Serverstart + sichtbarer Betriebshinweis bei fehlendem Scan-Cache. Keine Migration. Autoritativ: management/Bauplan_Build376_MigrationStatus_v0_1.md; mc 2026-07-10. ANLASS (echter Betriebsvorfall): Migration m009 (evidence_scan_cache, Build 374) war ausgeliefert, aber in der produktiven coordinator.db NIE ANGEWANDT \u2014 Migrationen laufen NICHT beim Serverstart, sondern ueber das eigene CLI 'python -m management.migrate'. Der Abnahmehinweis in Build 374 ('Server neu starten, m009 laeuft beim Start') war FALSCH. Folge im Betrieb: der Berichts-Scan-Cache fiel aus und protokollierte je Fall eine beilaeufige Warnung ('no such table: evidence_scan_cache'). WICHTIG: es ging nichts verloren - der Cache ist bewusst nur ein Beschleuniger, die Berichtsliste war vollstaendig. Aber der Zustand war leicht zu uebersehen. ENTSCHEIDUNG (mc): Der Server MIGRIERT NICHT SELBST. Das Anwenden bleibt eine kontrollierte, im Audit-Log belegte Handlung (deployed_by). Der Server WARNT nur - dafuer aber deutlich, an der sichtbarsten Stelle (Start), unter Nennung des exakten Befehls. (1) NEU management/server/migration_status.py (Grundregel 10: eigene Datei): MigrationStatus (Datenobjekt: applied/available/pending/ok/missing_registry) + MigrationStatusCheck (rein lesend: vergleicht schema_migrations mit den per discover() gefundenen Migrationsmodulen) + warning_lines() (mehrzeilige, umrahmte ACHTUNG-Warnung: nennt die ausstehenden Migrationen, die angewandten, die Folge, den exakten Befehl 'python -m management.migrate --deployed-by <KENNUNG>' und den Hinweis, dass der Server bewusst nicht selbst migriert). MIGRATE_COMMAND zentral definiert, damit Meldung und Doku nicht auseinanderlaufen. Fehlende Registry (uninitialisierte DB) wird eigens benannt. (2) GEAENDERT management/server/management_app.py: NEU migration_status() (rein lesend). Versionskopf -> 376. (3) GEAENDERT management.py: Schritt 3b nach dem RBAC-Start-Check - Migrationsstand pruefen; bei Rueckstand die Warnung auf stderr ausgeben, sonst '[management] Migrationsstand aktuell (N Migrationen).' Die Pruefung darf den Start nie verhindern, schweigt aber auch nicht (Fehler -> WARNUNG). (4) GEAENDERT management/reports/reports_repo.py: Cache-Schreibfehler wird nun EINMALIG gemerkt (statt je Fall zu loggen) und als 'cache_error' in der Antwort von list_reports() zurueckgegeben -> der Zustand wird SICHTBAR, statt im Log zu verschwinden. Die Berichtsliste bleibt dabei vollstaendig. (5) GEAENDERT management/server/static/cockpit_reports.js: Betriebshinweis in der Sicht, wenn cache_error gesetzt ist - nennt den Befehl 'python -m management.migrate' und stellt klar, dass die Liste dennoch VOLLSTAENDIG ist (nur jeder Aufruf liest alle Fall-DBs neu). Versionskopf -> 376. (6) NEU tests/test_migration_status.py (MS01-MS05): aktueller Stand -> keine Warnung; fehlende m009 (der exakte Produktionsfall) -> pending + Warnung nennt m009 UND den Befehl UND 'migriert BEWUSST NICHT selbst'; fehlende Registry; ManagementApp.migration_status(); MS05: bei geloeschter Cache-Tabelle liefert /api/reports die Berichte VOLLSTAENDIG und meldet cache_error sichtbar. (7) GEAENDERT tests/unit/test_cockpit_reports.test.js: BR07 - Betriebshinweis wird angezeigt und nennt den Befehl. REGRESSION run_tests.py: Python 987 passed (982+5) / 59 skipped / 3 subtests; JavaScript 565 passed (564+1) / 1 skipped / 1 todo (567), 49 Testdateien. SOFORTMASSNAHME IN DER VM (behebt den gemeldeten Vorfall, unabhaengig von diesem Build): 'python -m management.migrate --deployed-by h0a2898' ausfuehren, dann den Management-Server neu starten. Danach zeigt die Scan-Info beim zweiten Laden '0 neu eingelesen' (Cache greift). ABNAHME dieses Builds: Server starten -> bei vollstaendigem Stand erscheint '[management] Migrationsstand aktuell (N Migrationen).'; bei Rueckstand die umrahmte ACHTUNG-Warnung mit dem Befehl. NAECHSTER BUILD 377: Versiegelung (approved_reports.db, Inhaltshash nach der Konvention aus core/startup_checks.py OHNE report_comments, POST /api/report/approve, verify-Pfad). Danach 378: Schreibsperre im Ermittler-Webserver bei approved/final. HINWEIS: Bauplan-.md + cockpit.html-lose; nur die .md braucht 'git add -f'. GELIEFERT: siehe ZIP (per-file MD5)."
}

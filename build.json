{
  "build": 502,
  "version": "0.8.502",
  "date": "2026-07-24",
  "note": "Build 502 (AD-Abgleich Cockpit — Bauplan claude_Bauplan_Build501_502_ADSync_v0_1.md §7, mc 2026-07-24 E2 'beide Wege'). Basis v0.8.501. Zweiter Bedienweg zum Sync-Kern aus Build 501 (Migrationsklasse: migrationsneutral — kein Schema-Eingriff; nutzt M020 aus Build 501). GEAENDERT management/server/management_app.py: Konstruktor-Parameter ad_members_provider (F4-Muster, injizierbar; PROD lazy LdapGroupReader.from_config beim ERSTEN Aufruf — Server ohne ad.ldap-Konfiguration startet normal, der Konfigurationsfehler erscheint als Klartext in der Sicht). NEU GET /api/adsync (Vorschau, personnel.sync, mode=ro, KEIN Beleg; liefert Plan + Gruppe + SERVERSEITIGE Bestaetigungsworte als eine Wahrheitsquelle; LdapError -> 502 ldap_failed, leere/mehrdeutige AD-Antwort -> 502 ad_plan_invalid [Glitch-Schutz]). NEU POST /api/adsync/apply (Neuaufnahmen als investigator [Flag+person_role] + Namensaenderungen; Plan wird SERVERSEITIG frisch gebildet, kein Client-Plan wird akzeptiert). NEU POST /api/adsync/decide (deactivate/abort/reactivate je Kandidat; Bestaetigungswort 'Entfernen'/'Reaktivieren' wird SERVERSEITIG exakt geprueft; falsches Wort -> 400 confirmation_rejected OHNE automatischen Abbruch-Beleg — die Oberflaeche ist interaktiv, der protokollierte Abbruch ist die BEWUSSTE Aktion 'abort' [anders als der one-shot-CLI-Dialog; dokumentierte Entwurfsentscheidung]; nie Loeschen, nur is_active=0). NEU management/server/static/cockpit_adsync.js: Sicht 'AD-Abgleich' (IIFE, Debug-Logging, XSS: textContent; reine Funktionen counts/hasAutomatic/summaryText/confirmWords/validateWord/decideBody fuer vitest; Kandidaten-Zeilen mit Wort-Eingabe, 'Abbruch protokollieren'-Knopf mit Notiz, Apply-Knopf mit Doppelklick-Schutz; Browser-Vorpruefung des Worts ist NUR Komfort — verbindlich prueft der Server). GEAENDERT cockpit.js: Nav-Eintrag 'adsync' (personnel.sync, Gruppe Verwaltung) + loadAdSync; BEWUSST kein SSE-Auto-Reload (jeder Reload waere eine Live-LDAP-Anfrage). GEAENDERT cockpit.html (Script-Tag), cockpit.css (scoped .aiw-adsync-*; Deaktivierungs-Abschnitt als WARN markiert). TESTS NEU: tests/test_management_adsync_endpoint.py (AE01-AE07: 403 ohne Recht, Vorschau ohne Beleg, 502-Fehlerpfade, apply mit Belegkette, decide-Wortpruefung/abort/reactivate, bad requests), tests/unit/test_cockpit_adsync.test.js (ADS01-ADS09, JSDOM gegen den ECHTEN Code). TESTS ANGEPASST: tests/unit/test_cockpit_nav.test.js (Katalog-Anker 29 -> 30, 'adsync'). REGRESSION: volle Python-Suite gruen unter Python 3.13 (1723 passed, 54 skipped, 6 subtests; Basis 501: 1716); volle vitest-Suite gruen (1041 passed, 87 Dateien; Basis 501: 1032/86); ast.parse + node --check aller geaenderten Dateien OK. OFFEN (kein stiller Verzicht): Grant von personnel.sync an 'supervisor' ist operative Entscheidung via policy_admin (default-deny); Aufnahme der M020 in den Datenmigrationsleitfaden nach Abnahme; is_active-Filter in Zuweisungs-/Workload-Sichten als Folgebuild."
}

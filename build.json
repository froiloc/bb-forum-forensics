{
  "build": 359,
  "version": "0.7.359",
  "date": "2026-07-10",
  "note": "Build 359 (Baustelle 7, Welle 0): KAPAZITAET TEIL 4a - Aggregat-Endpunkt (Backend der Cockpit-Sicht). '/api/capacity' erweitert; keine Migration. Split (mc 2026-07-10): Frontend-Sicht -> Build 360. Autoritativ: Bauplan_Baustelle7_Management_v1_1 Paragraph 11.4; management/Bauplan_Build359_CapacityAggregate_v0_1.md; mc (Endpunkt wiederverwenden; Auslastungs-Faerbung netto/basis in 360; Split 359/360). (1) GEAENDERT management/server/management_app.py: _capacity erweitert. Query start/end (Pflicht) + OPTIONAL person_id. MIT person_id -> Einzelperson (flaches CapacityResult, unveraendert seit Build 358). OHNE person_id -> AGGREGAT: je Ermittler (person.is_investigator=1, ORDER BY id) eine Kapazitaets-Zeile (CapacityResult + system_username + display_name fuer die Sicht). scope-aware: 'alle' -> alle Ermittler; 'eigene' -> nur die eigene Zeile. Antwort {scope, count, start, end, capacities:[...]}. 400 bei fehlenden start/end; CapacityError -> 400. Weiterhin read-only, gegatet auf capacity.edit. Versionskopf -> 359. (2) GEAENDERT tests/test_capacity_calculator.py: EP05 Aggregat alle (3 Ermittler, person 2 netto 2400, 1/3 netto 0, Anzeigename mitgeliefert), EP06 Aggregat scope eigene (nur eigene Zeile), EP07 Aggregat ohne start/end -> 400. EP01-EP04 (Einzelperson) unveraendert gruen. REGRESSION run_tests.py: Python 940 passed (937+3) / 59 skipped / 3 subtests; JavaScript 511 passed (unveraendert) / 1 skipped / 1 todo (513), 40 Testdateien. ABNAHME: nach Grant capacity.edit GET '/api/capacity?start=YYYY-MM-DD&end=YYYY-MM-DD' (ohne person_id) -> {capacities:[{person_id, display_name, basis, einschraenkungen, garantie_boden, netto, ...}]}. NAECHSTER BUILD 360 (Frontend, console-first): cockpit_capacity.js (ECharts, Basis vs. netto je Ermittler, Auslastungs-Faerbung netto/basis, Zeitraumwahl) + cockpit.js/cockpit.html-Verdrahtung + SSE-Reload; vitest fuer die reine Serien-Logik. HINWEIS: nur die Bauplan-.md ist per .gitignore untracked -> 'git add -f'. GELIEFERT: siehe ZIP (per-file MD5)."
}

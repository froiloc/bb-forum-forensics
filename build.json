{
  "build": 344,
  "version": "0.7.344",
  "date": "2026-07-10",
  "note": "Build 344 (Baustelle 7, Welle 0, RBAC Schnitt b): RBAC-Schreibpfad + policy-CLI. Additiv (nur neuer Code + 4 additive event_types; keine Schemaaenderung). Autoritativ: Bauplan_Baustelle7_Management_v1_1 Paragraph 11.1/11.3/11.7 (Schnitt b), management/Bauplan_Build344_RBAC_Writepath_v0_1.md; mc 2026-07-10. KERN (1) NEU management/rbac/rbac_repo.py (Klassen RbacRepo, RbacError): auditierter Schreibpfad ueber das CoordinatorWriter-Gateway mit audit_seq-Kopplung wie case_events (after_audit-Hook). Methoden: grant(role, capability, scope, actor_id, note) -> RBAC_GRANTED, rbac_grant-Zeile mit audit_seq==seq; revoke_grant(grant_id, actor_id, note) -> RBAC_REVOKED, Soft-Revoke (revoked_at/by + revoke_audit_seq, KEIN DELETE); assign_role(person_id, role_code, actor_id) -> ROLE_ASSIGNED; revoke_role(person_role_id, actor_id) -> ROLE_REVOKED, Soft-Revoke; Leser list_grants/list_person_roles (active_only), get_grant. Katalog-Validierung gegen catalog.ROLE_CODES/CAPABILITY_CODES + scope in alle|eigene|None (ungueltig -> RbacError, kein Write/Audit). Logische Eindeutigkeit je aktivem Schluessel: zweiter aktiver Grant (role,capability) bzw. zweite aktive Zuweisung (person,role) -> RbacError; Scope-Aenderung per revoke-then-grant (append-only). Alle Guards INNERHALB der Schreibsperre (kein TOCTOU). (2) NEU management/rbac/rbac_admin.py (CLI python -m management.rbac.rbac_admin): Subkommandos catalog|grant|revoke-grant|assign-role|revoke-role|list-grants|list-roles. Muster wie person_admin (_resolve_db_path, _open_con, _resolve_actor: --actor SYSUSER -> actor_id, sonst System + meta.performed_by). Person-Aufloesung per --person SYSUSER oder --person-id. (3) GEAENDERT management/audit/event_types.py: 4 additive Werte RBAC_GRANTED 'rbac_granted', RBAC_REVOKED 'rbac_revoked', ROLE_ASSIGNED 'role_assigned', ROLE_REVOKED 'role_revoked' (in Klasse + ALL-Frozenset; eingefrorenes Vokabular nur ERWEITERT, nichts umbenannt/entfernt). (4) NEU tests/test_management_rbac_repo.py (G01-G11 + A01, 12 Tests): grant atomar+gekoppelt; ungueltige Codes/Scope Rollback; Duplikat-Guard; revoke_grant Soft-Revoke+gekoppelt; revoke-Fehler; assign_role atomar; assign-Fehler (Person/Rolle/Duplikat); revoke_role Soft; verify_chain gruen; list active_only-Semantik; default-deny-Ausgangszustand; CLI grant+list-grants Ende-zu-Ende. ENTSCHEIDUNG (mc 2026-07-10): 344 ist WERKZEUG-ONLY (faithful zu Paragraph 11.7 b 'Schreibpfad + policy-CLI'). KEINE Basis-Grants im Build gebacken - operative, auditierte Daten entstehen zur Deploy-Zeit ueber die CLI (Grundsatz wie bei person: keine Migrations-Seeds fuer auditierte Betriebsdaten). Eine EMPFOHLENE Basis-Matrix aus Paragraph 11.3 liegt als dokumentierte CLI-Sequenz im Detail-Bauplan (versioniert, reproduzierbar, NICHT automatisch ausgefuehrt; Policy-Entscheidung bleibt bei der Chef-Ermittlerin). BEWUSST NICHT ENTHALTEN: Resolver/Durchsetzung/Start-Check 'Code-Capability in DB' (Schnitt c, Folge-Build); Management-Server/SSE (spaeter). REGRESSION run_tests.py: Python 833 passed (821+12) / 59 skipped / 3 subtests, JavaScript 467 passed / 1 skipped / 1 todo (kein JS geaendert). DEPLOY: keine Migration in diesem Build (m006-Schema aus 343 genuegt); die Basis-Matrix wird per rbac_admin-CLI gesetzt (auditiert, coordinator.db). HINWEIS: Bauplan-Detaildatei liegt im ZIP, per .gitignore (*.md) nicht getrackt -> 'git add -f'. GELIEFERT: siehe ZIP (per-file MD5)."
}

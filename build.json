{
  "build": 360,
  "version": "0.7.360",
  "date": "2026-07-10",
  "note": "Build 360 (Baustelle 7, Welle 0): KAPAZITAET TEIL 4b - Cockpit-Sicht (ECharts-Frontend). Frontend-Teil des Split (Aggregat-Endpunkt = 359). Reiner Frontend-Build (kein Backend/keine Migration). SCHLIESST DIE KAPAZITAET AB (355-360). Autoritativ: Bauplan_Baustelle7_Management_v1_1 Paragraph 11.4; management/Bauplan_Build360_CapacityView_v0_1.md; mc 2026-07-10 (Basis vs. netto je Ermittler + Zeitraumwahl; Ueberlast vorerst als Auslastungs-Faerbung netto/basis; Split 359/360). DEPLOY-HINWEIS: cockpit.html geaendert -> .gitignore '*.html' -> 'git add -f management/server/static/cockpit.html'. (1) NEU management/server/static/cockpit_capacity.js (IIFE, Live-DEBUG, UMD -> vitest; window.AIWCockpitCapacity): rein nameLabel, utilization (netto/basis; null bei basis<=0), utilColor (Auslastungs-Faerbung: >=0.8 gruen, >=0.5 gelb, sonst rot; grau ohne Basis; Farben spiegeln cockpit.css), sortRows (stark reduzierte zuerst, basis-lose ans Ende, stabil), echartsOption (zwei Serien Basis grau + Netto je Balken gefaerbt; yAxis.inverse), defaultPeriod(now) (laufender Monat erster..letzter Tag; now injizierbar), scopeText; DOM renderCapacity(mainEl, data, {ECharts, onPeriodChange}) (Kopf + Zeitraum-Wahlfelder + Button, der onPeriodChange(start,end) ausloest, + ECharts-Instanz; ECharts injizierbar). (2) GEAENDERT management/server/static/cockpit.js: 'capacity'->'capacity.edit' im VIEW_CATALOG; loadCapacity(mainEl, period) holt /api/capacity (Aggregat, ohne person_id) fuer den Zeitraum, rendert via AIWCockpitCapacity, haelt state.capacityPeriod (Default: defaultPeriod = laufender Monat) fuer SSE-Reload; Zeitraum-Wahl (onPeriodChange) laedt mit neuem Zeitraum neu; ECharts-Lifecycle ueber cleanupView (state.chart, dispose+Resize). selectView 'capacity'-Zweig; SSE 'changed' laedt aktive Kapazitaets-Sicht mit demselben Zeitraum neu. (3) GEAENDERT management/server/static/cockpit.html: cockpit_capacity.js (defer) eingebunden. (4) NEU tests/unit/test_cockpit_capacity.test.js (CA01-CA08, JSDOM): utilization, utilColor-Schwellen, sortRows, echartsOption (Serien/Farben/inverse), defaultPeriod, renderCapacity (Kopf/Zeitraum, Stub-ECharts, onPeriodChange bei Klick), ohne ECharts -> null+Hinweis. (5) GEAENDERT tests/unit/test_cockpit_nav.test.js: VIEW_CATALOG-Laenge 11 -> 12 (neue capacity-Sicht; erwartetes Pinning). REGRESSION run_tests.py: Python 940 passed (unveraendert - reines Frontend) / 59 skipped / 3 subtests; JavaScript 519 passed (511+8) / 1 skipped / 1 todo (521), 41 Testdateien (40+1). BROWSER-ABNAHME (console-first): capacity.edit granten (supervisor hat es i.d.R.), Cockpit laden -> Tab 'Kapazitaet' zeigt je Ermittler Basis- und Netto-Balken fuer den laufenden Monat; Netto nach Auslastung gefaerbt; Zeitraum aenderbar; SSE-Reload ohne F5. Bei Auffaelligkeiten window.AIW_COCKPIT_DEBUG=true -> Console -> PoC -> Fix. STAND: KAPAZITAET KOMPLETT (355 Schema, 356 Worktime/Holiday, 357 Reason/Availability, 358 Berechnung, 359 Aggregat, 360 Sicht). Welle-0-Rest laut Bauplan abgearbeitet (Backup + Kapazitaet). HINWEIS: Bauplan-.md + cockpit.html per .gitignore -> 'git add -f'. GELIEFERT: siehe ZIP (per-file MD5)."
}

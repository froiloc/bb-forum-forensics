{
  "build": 463,
  "version": "0.7.463",
  "date": "2026-07-20",
  "note": "Build 463 (AP-2G Betrieb/Governance, Externe Fallfreigabe, Frontend zu Build 462; mc 2026-07-20; MD5-Handshake bestaetigt). REINES FRONTEND; kein Endpunkt/DB/Migration. Cockpit-Sicht 'releases' (Gruppe Administration, Recht release.view) fuer die externen Fallfreigaben an NRW-Ermittler mit Freigabe/Widerruf (Recht release.grant). NEU management/server/static/cockpit_releases.js: IIFE + 'use strict'; DEV-Logging (AIW_COCKPIT_DEBUG); reine Helfer (countsModel/statusDotClass/allowedRevoke/releaseRows/recipientOptions/umfangOptions); renderReleases() zeigt Kennzahlen, ein Freigabe-Formular (Fall/Empfaenger aus F4-Allowlist/Umfang/Unbedenklichkeits-Grundlage Pflicht) und die Freigaben-Tabelle; Widerruf ueber Grund-Panel (Pflicht, rote Endgueltig-Warnung); leere Empfaenger-Allowlist -> deutlicher Default-Deny-Hinweis statt leerem Formular; XSS-sicher via textContent; UMD-Ausgang window.AIWCockpitReleases. GEAENDERT management/server/static/cockpit.js: VIEW_CATALOG-Eintrag 'releases' (release.view); loadReleases() (GET /api/releases; onGrant->POST /api/release/grant, onRevoke->POST /api/release/revoke mit X-AIW-Token; KEIN optimistisches UI - Reload + pendingMsg); selectView-Dispatch + SSE-'changed'-Reload. GEAENDERT management/server/static/cockpit.html: <script> cockpit_releases.js. GEAENDERT management/server/static/cockpit.css: NEU .dot.grau (neutraler Ampelpunkt) + eng gefasste .aiw-rel-* Stile (Formular, Tabelle, gelbes Widerruf-Panel, rote Warnung, Ergebniszeile). TESTS: tests/unit/test_cockpit_releases.test.js (RL01-RL07: Helfer, Rendern mit/ohne Recht, Grant-Validierung, Default-Deny bei leerer Allowlist, Widerruf-Grund-Pflicht, kein Button an widerrufener Freigabe, XSS-sicher); GEAENDERT tests/unit/test_cockpit_nav.test.js (VIEW_CATALOG 24->25). REGRESSION: node --check OK; volle vitest 883 passed/1 skip/1 todo; volle pytest 1518 passed/49 skipped (unveraendert - reines Frontend). LIVE-VERIFIKATION IN DER VM EMPFOHLEN (Freigabe/Widerruf im echten Browser; ad.release_recipients in config.yaml + Grants gesetzt). Damit ist die externe Fallfreigabe vollstaendig (462 Backend + 463 Frontend). Buildnummer ggf. manuell anpassen, falls 463 parallel vergeben."
}

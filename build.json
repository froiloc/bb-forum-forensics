{
  "build": 378,
  "version": "0.7.378",
  "date": "2026-07-10",
  "note": "Build 378 (Baustelle 7): BERICHTS-FREIGABE im Cockpit (Frontend). Reiner Frontend-Build (kein Backend, keine Migration) - die Endpunkte kamen in Build 377. Autoritativ: management/Bauplan_Build378_ApprovalView_v0_1.md. DEPLOY-HINWEIS: cockpit.html geaendert -> .gitignore '*.html' -> 'git add -f management/server/static/cockpit.html'. (1) GEAENDERT management/server/static/cockpit_reports.js: toRows liefert nun report_id (fuer die Aktionen zwingend). NEU rein: availableActions(row, canApprove) - spiegelt die SERVER-Vorbedingungen (Build 377): 'Freigeben' nur aus status 'submitted' UND nur mit reports.approve; 'Endgueltig freigeben' nur aus 'approved'; 'Siegel pruefen' bei 'approved'/'final' (auch OHNE reports.approve - pruefen darf, wer die Berichte sehen darf). NEU rein: verifyText(v) - benennt eine ABWEICHUNG beim Namen ('Manipulationsverdacht'), beschoenigt nichts. DOM: AKTIONSFELD unter der Tabelle (Zeilenklick waehlt den Bericht) mit den verfuegbaren Knoepfen + Ergebnisbereich; showVerify() stellt das Pruefergebnis dar (Klartext + Gegenueberstellung Siegel-Hash vs. aktueller Hash, Freigeber/Zeit/Beleg/Endgueltig). BEWUSST KEINE Knoepfe in den Tabellenzellen: die Freigabe ist ein Schreibvorgang mit Belegpflicht - erst Bericht waehlen, dann handeln (ein Versehen weniger). XSS: nur textContent. (2) GEAENDERT management/server/static/cockpit.js: loadReports(mainEl, force, pendingMsg) verdrahtet nun canApprove (aus den Faehigkeiten; der Server prueft ohnehin erneut - die Oberflaeche soll nur keine Aktion anbieten, die zwingend scheitern wuerde), onApprove (POST /api/report/approve mit Schreib-Token; danach NEU LADEN - kein optimistisches UI; Rueckmeldung nennt Beleg-Nummer und die ersten Stellen des Inhaltshashes) und onVerify (GET /api/report/verify -> Ergebnis im Aktionsfeld). Fehler werden im Aktionsfeld sichtbar gemeldet (Grundregel 1). (3) GEAENDERT management/server/static/cockpit.html: Versionskopf. (4) GEAENDERT tests/unit/test_cockpit_reports.test.js: BR08 (availableActions spiegelt die Vorbedingungen exakt: submitted->approve nur mit Cap; approved->final+verify; draft->nichts; final->nur verify; ohne Cap bleibt nur verify), BR09 (verifyText nennt ABWEICHUNG + Manipulationsverdacht), BR10 (Zeilenklick oeffnet das Aktionsfeld, Knoepfe rufen die Callbacks mit den richtigen Argumenten, showVerify stellt die ABWEICHUNG samt Hash-Gegenueberstellung dar). REGRESSION run_tests.py: Python 994 passed (unveraendert - reines Frontend) / 59 skipped / 3 subtests; JavaScript 568 passed (565+3) / 1 skipped / 1 todo (570), 49 Testdateien. BROWSER-ABNAHME (console-first): Server NEU STARTEN. Voraussetzung: ein Bericht mit status='submitted' in einer evidence-DB. Cockpit -> 'Berichts-Abnahme' -> Zeile anklicken -> Aktionsfeld erscheint. 'Freigeben (versiegeln)' -> Meldung 'Freigegeben und versiegelt (Beleg #N, Hash ...)'; die Zeile steht danach auf 'freigegeben'. Dann Zeile erneut waehlen -> 'Siegel pruefen' -> 'Siegel in Ordnung'. GEGENBEWEIS: den Bericht direkt in der evidence-DB aendern (SQLite-Werkzeug, am Werkzeug vorbei), dann erneut 'Siegel pruefen' -> 'ABWEICHUNG ... Manipulationsverdacht' samt Hash-Gegenueberstellung. Bei Auffaelligkeiten window.AIW_COCKPIT_DEBUG=true -> Console -> PoC -> Fix. STAND: Berichts-Abnahme komplett (374 Scan, 375 Sicht, 377 Versiegelung, 378 Freigabe-Frontend). Welle 1 damit abgeschlossen. NAECHSTER BUILD 379: SCHREIBSPERRE im ERMITTLER-Webserver bei status approved/final (Durchsetzung dort, wo geschrieben wird - bisher schuetzt nur der zentrale Hash NACHTRAEGLICH). Danach: Fall-AUTODETEKTION gegen data/ (neue evidence-DBs aufnehmen, fehlende melden). OFFEN/VERMERKT: Fallauswahl-GUI fuer Ermittler (Ermittler-Webserver; Behelf main.py --user-id). HINWEIS: Bauplan-.md + cockpit.html per .gitignore -> 'git add -f'. GELIEFERT: siehe ZIP (per-file MD5)."
}

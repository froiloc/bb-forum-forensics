{
  "build": 356,
  "version": "0.7.356",
  "date": "2026-07-10",
  "note": "Build 356 (Baustelle 7, Welle 0): KAPAZITAET TEIL 2a - Schreibpfade Arbeitszeit + Feiertage (WorktimeRepo/HolidayRepo) + capacity_admin-CLI. Keine Migration (schreibt auf m008-Schema). Split (mc 2026-07-10): Reason+Availability -> 357. Autoritativ: Bauplan_Baustelle7_Management_v1_1 Paragraph 11.4; management/Bauplan_Build356_CapacityWriteWorktimeHoliday_v0_1.md; mc (4 Repos; Worktime append-only ohne Auto-effective_to; Split in zwei Builds). (1) NEU management/capacity/__init__.py + capacity_errors.py (CapacityError - klarer Fehler statt stillem No-op). (2) NEU management/capacity/worktime_repo.py (WorktimeRepo): set_worktime(person, effective_from, mon..sun-Minuten, [effective_to], actor) -> WORKTIME_SET-Beleg; APPEND-ONLY (neue datierte Zeile, KEINE automatische Schliessung der Vorgaengerzeile - Entscheidung 2); Zeile traegt audit_seq == Beleg-seq (after_audit-Hook). Validierung: Minuten >=0 und <=1440 (sonst CapacityError). list_worktime(person, include_deleted) sortiert nach person/effective_from. (3) NEU management/capacity/holiday_repo.py (HolidayRepo): add_holiday(day, label, region, actor) -> HOLIDAY_ADDED (Duplikat-Guard fuer aktive (day, region), null-sicher via 'IS ?'); remove_holiday(id, actor) -> HOLIDAY_REMOVED (SOFT-DELETE deleted_at, kein hartes DELETE; nicht vorhanden/bereits entfernt -> CapacityError, kein stiller No-op). Hinweis: Schema fuehrt bewusst keine delete_audit_seq-Spalte (Paragraph 11.4); der Entfernungs-Beleg steht im audit_log mit target_id=holiday_id. list_holidays(region, include_deleted). (4) NEU management/capacity/capacity_admin.py (CLI, main(argv) testbar): set-worktime/list-worktime, add-holiday/remove-holiday/list-holidays. Pfad aus config.yaml (paths.coordinator_db) oder --coordinator-db; --actor; --person-id oder --person (system_username). Muster wie rbac_admin. (5) NEU tests/test_capacity_worktime_holiday.py (WT01-WT03, HL01-HL04, CL01): audit_seq-Kopplung (worktime_set/holiday_added/holiday_removed), append-only (zwei Regeln -> zwei Zeilen), negative Minuten -> Fehler, Soft-Delete (deleted_at gesetzt, Zeile bleibt, nicht in aktiver Liste), remove auf fehlender id -> Fehler, Duplikat-Guard (gleiche day+region doppelt -> Fehler, andere Region ok), CLI End-to-End (set-worktime + add/remove-holiday). REGRESSION run_tests.py: Python 918 passed (910+8) / 59 skipped / 3 subtests; JavaScript 511 passed (unveraendert) / 1 skipped / 1 todo (513), 40 Testdateien. CLI: python -m management.capacity.capacity_admin {set-worktime|list-worktime|add-holiday|remove-holiday|list-holidays} [--config ./config.yaml] [--actor h0a2898]. NAECHSTE BUILDS: 357 ReasonRepo + AvailabilityRepo (+CLI-Erweiterung); 358 Kapazitaets-Berechnung; 359 Cockpit-Sicht. HINWEIS: nur die Bauplan-.md ist per .gitignore untracked -> 'git add -f'. GELIEFERT: siehe ZIP (per-file MD5)."
}

{
  "build": 368,
  "version": "0.7.368",
  "date": "2026-07-10",
  "note": "Build 368 (Baustelle 7, Welle 1): ERMITTLER-BETREUUNG TEIL 1 - Backend /api/mentoring (LIVE-Sicht). Keine Migration. Split (mc 2026-07-10): Frontend -> Build 369. Autoritativ: Ideen_Verwaltungswerkzeug_konsolidiert Paragraph 2.12 ('laufende Support-Sitzungen begleiten'); support_sessions-Live-Tabelle; DEFAULT_SUPPORT_STALE_SEC=30; management/Bauplan_Build368_MentoringBackend_v0_1.md; mc (Live-Sicht; Live-Tabelle als Quelle; scope eigene = nur eigene; stale zuerst). (1) GEAENDERT management/support_sessions/support_sessions_repo.py: NEU list_running() - ALLE laufenden Sitzungen (ended_at IS NULL), falluebergreifend, LEFT JOIN person (Supporter) + cases (username). Read-only, KEINE Stale-Filterung (Bewertung beim Aufrufer). Aufsteigend nach started_at. (2) GEAENDERT management/server/management_app.py: import time; CAP_MENTORING='mentoring.view'; Import SupportSessionsRepo + DEFAULT_SUPPORT_STALE_SEC; Dispatch '/api/mentoring' -> _mentoring. _mentoring: cap-Check (403); laedt SupportSessionsRepo(con, None).list_running(); annotiert je Sitzung heartbeat_age_sec, started_ago_sec, live (Alter <= stale_sec). scope 'alle' -> alle laufenden; 'eigene' -> nur supporter_id==ich. Sortierung: stale (live=False) zuerst, dann aeltester Start. Antwort {scope, stale_sec, count, sessions:[...]}. Read-only. Versionskopf -> 368. (3) NEU tests/test_mentoring_view.py (MN01-MN04): nur laufende (beendete nicht) + Live/Stale-Flag; scope eigene (nur eigene laufende); stale-zuerst-Sortierung; 403 ohne Cap. REGRESSION run_tests.py: Python 960 passed (956+4) / 59 skipped / 3 subtests; JavaScript 540 passed (unveraendert - Backend-only) / 1 skipped / 1 todo (542), 45 Testdateien. ABNAHME: nach Grant mentoring.view GET '/api/mentoring' -> {sessions:[{id, user_id, username, supporter_*, started_at, last_heartbeat, heartbeat_age_sec, started_ago_sec, live}]}, stale zuerst. Supervisor (alle) alle; support (eigene) nur eigene. NAECHSTER BUILD 369 (Frontend, console-first): cockpit_mentoring.js (Tabulator, Live/Stale-Ampel, Laufzeit), PERIODISCHER Refresh (Heartbeats sind NICHT auditiert -> SSE reicht nicht; Session-Start/-Ende bleiben SSE-getrieben) + Verdrahtung. HINWEIS: nur die Bauplan-.md ist per .gitignore untracked -> 'git add -f'. GELIEFERT: siehe ZIP (per-file MD5)."
}

{
  "build": 351,
  "version": "0.7.351",
  "date": "2026-07-10",
  "note": "Build 351 (Baustelle 7, Welle 1): LASTVERTEILUNG FRONTEND (ECharts-Sicht). Frontend-Teil des Split (Backend /api/workload + ECharts-Vendoring = 350). Reiner Frontend-Build (kein Backend/keine Migration). Autoritativ: Bauplan_Baustelle7_Management_v1_1 Paragraph 11.2; management/Bauplan_Build351_WorkloadFrontend_v0_1.md; mc 2026-07-10 (Chart = horizontal gestapelte Ampel-Balken je Ermittler, Rueckstau eigene Zeile, rot desc). DEPLOY-HINWEIS: cockpit.html geaendert -> faellt unter .gitignore '*.html' -> muss mit 'git add -f management/server/static/cockpit.html' eingecheckt werden. (1) NEU management/server/static/cockpit_workload.js (IIFE, Live-DEBUG, reine Funktionen UMD-exportiert -> vitest; window.AIWCockpitWorkload): rein nameLabel, echartsOption(data) (deterministische ECharts-Option: 3 gestapelte Serien Rot/Gelb/Gruen ueber Ermittler-Kategorien; yAxis.inverse=true -> dringlichster oben; Backend-Reihenfolge ROT desc + Rueckstau ans Ende beibehalten), scopeText; DOM renderWorkload(mainEl, data, {ECharts}) (Kopf/Scope/Count + ECharts-Instanz; Container-Hoehe waechst mit Zeilenzahl; ECharts-Ctor injizierbar -> vitest). FARB-VERTRAG: Ampelfarben spiegeln cockpit.css (--rot/--gelb/--gruen) als Konstanten (ECharts liest keine CSS-Variablen). (2) GEAENDERT management/server/static/cockpit.js: selectView 'workload'-Zweig -> loadWorkload(); loadWorkload holt /api/workload, rendert via AIWCockpitWorkload, bindet Resize-Handler an die ECharts-Instanz. NEU state.chart/state.chartResize; destroyChart() (ECharts dispose() + Resize abmelden) und cleanupView() (destroyTable + destroyChart) ersetzen die bisherigen destroyTable-Aufrufe in selectView/loadOverview (sauberer Lifecycle: Tabelle UND Diagramm werden beim Sichtwechsel/Reload abgebaut). SSE 'changed' laedt auch die aktive Workload-Sicht neu. (3) GEAENDERT management/server/static/cockpit.html: echarts.min.js + cockpit_workload.js (defer, Reihenfolge) eingebunden. (4) NEU tests/unit/test_cockpit_workload.test.js (WL01-WL07, JSDOM): nameLabel-Fallbacks, echartsOption (Kategorien in Backend-Reihenfolge, 3 Serien mit Werten, yAxis.inverse, Farben, stack, leere loads), renderWorkload (Kopf/Scope/Count, Stub-ECharts.init+setOption erhalten die Option, ohne ECharts -> null+Hinweis). REGRESSION run_tests.py: Python 883 passed (unveraendert - reines Frontend) / 59 skipped / 3 subtests; JavaScript 511 passed (504+7) / 1 skipped / 1 todo (513), 40 Testdateien (39+1). BROWSER-ABNAHME (console-first): In Prod zuerst workload.view granten, z.B. 'python -m management.rbac.rbac_admin grant --role supervisor --capability workload.view --scope alle --actor h0a2898 --coordinator-db data/coordinator.db' und der Testperson die Rolle (bereits supervisor). Dann Cockpit laden -> Tab 'Lastverteilung' zeigt das gestapelte Ampel-Balkendiagramm (dringlichster oben, Rueckstau als eigener Balken). SSE: auditierte Aenderung -> Diagramm laedt ohne F5 neu. Bei Auffaelligkeiten: Console-Output (window.AIW_COCKPIT_DEBUG=true) -> PoC -> Fix. HINWEIS: Bauplan-.md per .gitignore untracked -> 'git add -f'. GELIEFERT: siehe ZIP (per-file MD5)."
}

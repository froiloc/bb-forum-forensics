{
  "build": 373,
  "version": "0.7.373",
  "date": "2026-07-10",
  "note": "Build 373 (Baustelle 7, Welle 1): ZUWEISUNG TEIL 2 - Cockpit-SCHREIB-Sicht (Frontend). Frontend-Teil des Split (Backend = 372). Keine Migration. Zusaetzlich: Korrektur der irrefuehrenden Server-Startmeldung (Hinweis des Entwicklers). Autoritativ: Bauplan_Baustelle7_Management_v1_1 Paragraph 2.6; management/Bauplan_Build373_AssignmentView_v0_1.md. DEPLOY-HINWEIS: cockpit.html geaendert -> .gitignore '*.html' -> 'git add -f management/server/static/cockpit.html'. (1) NEU management/server/static/cockpit_assignment.js (IIFE, Live-DEBUG, UMD; window.AIWCockpitAssignment): rein assigneeLabel, toRows, investigatorOptions (inkl. '(nicht zugewiesen)' + aktuelle Last je Ermittler), changeRequest(kind, userId, value) -> {path, body} fuer assign/priority/status ('' -> person_id null = entziehen); DOM renderAssignment(mainEl, data, {onChange}) - Tabelle aller Faelle mit DREI Auswahlfeldern je Zeile (Ermittler / Prioritaet / Status) + Rueckmeldebereich; gibt {setMessage} zurueck. KEIN OPTIMISTISCHES UI (bewusst, forensisch): die Oberflaeche zeigt nie einen Zustand, der nicht bestaetigt geschrieben ist. XSS: nur textContent/Option.text. (2) GEAENDERT management/server/static/cockpit.js: state.writeToken (aus /api/whoami beim Boot uebernommen; Warnung im DEBUG, falls fehlend); NEU postJson(url, body) - setzt Content-Type application/json + Header 'X-AIW-Token', wertet Fehlerantworten AUS und reicht die Server-Begruendung als Fehler weiter (Grundregel 1: kein stiller Fehlschlag); loadAssignment(mainEl, pendingMsg) - laedt /api/assignable, rendert, schreibt bei Aenderung per POST und LAEDT DANACH NEU; die Rueckmeldung (Erfolg 'Gespeichert (Beleg #<audit_seq>)' ODER Fehler) wird ueber pendingMsg durch den Reload getragen und bleibt sichtbar; bei Fehler wird ebenfalls neu geladen, damit keine ungeschriebene Auswahl stehen bleibt. selectView 'assignment'-Zweig; SSE 'changed' laedt die Zuweisungs-Sicht neu. (assignment war bereits im VIEW_CATALOG -> kein Nav-Count-Change.) (3) GEAENDERT management/server/static/cockpit.html: cockpit_assignment.js (defer) eingebunden. (4) GEAENDERT management.py: STARTMELDUNG korrigiert. Bisher '[management] Server laeuft (read-only): <url>' - seit Build 372 sachlich FALSCH und irrefuehrend (auch fuer Pruefer). Neu: '[management] Server laeuft: <url>' + Zeile 'Lesezugriffe read-only; Schreibzugriffe nur ueber die auditierten POST-Routen (Token-geschuetzt, jede Aenderung wird im audit_log belegt).' (5) NEU tests/unit/test_cockpit_assignment.test.js (AZ01-AZ06, JSDOM): API; toRows/assigneeLabel; investigatorOptions; changeRequest (assign/priority/status, '' -> null, unbekannt -> null); renderAssignment (2 Zeilen x 3 Auswahlfelder, Aenderung ruft onChange mit korrekten Argumenten); setMessage (Erfolg/Fehler sichtbar). REGRESSION run_tests.py: Python 973 passed (unveraendert - Frontend + reine Textaenderung in management.py) / 59 skipped / 3 subtests; JavaScript 557 passed (551+6) / 1 skipped / 1 todo (559), 48 Testdateien (47+1). BROWSER-ABNAHME (console-first): Server NEU STARTEN. assignment.edit mit Scope 'alle' granten -> Cockpit laden -> Tab 'Zuweisung': Tabelle aller Faelle; Ermittler/Prioritaet/Status per Auswahlfeld aendern -> Meldung 'Gespeichert (Beleg #N)'; der Beleg ist im audit_log nachpruefbar (auch in 'Meine Historie' sichtbar). Fehlerfall pruefen: Server neu starten OHNE Seiten-Reload -> Schreibversuch scheitert mit Token-Fehlermeldung (altes Token) - Seite neu laden behebt es. Bei Auffaelligkeiten window.AIW_COCKPIT_DEBUG=true -> Console -> PoC -> Fix. STAND: Zuweisung komplett (372 Backend, 373 Frontend). Damit sind ALLE Welle-1-Cockpit-Sichten verdrahtet AUSSER 'Berichts-Abnahme' (reports.approve). OFFEN/VERMERKT: Fallauswahl-GUI fuer Ermittler (eigener Baustein am ERMITTLER-Webserver; Behelf: main.py --user-id). HINWEIS: Bauplan-.md + cockpit.html per .gitignore -> 'git add -f'. GELIEFERT: siehe ZIP (per-file MD5)."
}

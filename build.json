{
  "build": 346,
  "version": "0.7.346",
  "date": "2026-07-10",
  "note": "Build 346 (Baustelle 7, Welle 0, Schritt 3): Management-Server BACKEND (read-only). Erster Build mit Browser-Anteil; Split analog 314/315 - das vollstaendig pytest-testbare Backend jetzt, die gerenderte Cockpit-Oberflaeche folgt in einem browser-verifizierbaren Build. Autoritativ: Bauplan_Baustelle7_Management_v1_1 Paragraph 11.2 (+11.6, 10.6), management/Bauplan_Build346_ManagementServer_v0_1.md; mc 2026-07-10 (fahre fort). KERN (1) NEU management.py (Entry-Point, analog main.py, getrennt vom Forensik-Webserver): Args (--coordinator-db/--config/--host/--port/--auto-port/--open-browser/--as-user); Ablauf DB-Pfad -> Start-Check (verify_catalog_present) -> Identitaet (IdentityResolver) -> localhost-Port (fest/auto) -> Server -> optional Browser -> serve_forever. Bindet nur localhost (Warnung sonst). READ-ONLY, kein CoordinatorWriter, keine Migration. (2) NEU management/server/identity.py (IdentityResolver, IdentityError): OS-Benutzer (SAMAccountName) -> person.system_username (stabile forensische Identitaet; AD-Anzeigename = display_name). Gekapselt, MOCKBAR (os_user_source injizierbar; Default getpass.getuser; --as-user Override). Rein lesend. Unbekannt -> IdentityError (kein stiller Fallback). (3) NEU management/server/management_app.py (ManagementApp, Response-DTO, format_sse_event): TESTBARER KERN ohne Socket. dispatch(person_id, path, query) -> Response. Endpunkte: / (Platzhalter-Shell, HTML), /api/whoami (Identitaet + aufgeloeste Rollen/Faehigkeiten), /api/overview (DashboardRepo, gate dashboard.view; Scope 'alle'=alle Faelle, sonst nur eigene Zuweisungen assigned_to==person_id), /api/integrity (gate ops.view; verify_chain + tip_seq), /static/* -> 404 (Cockpit-Frontend folgt), sonst 404. Fehlende Faehigkeit -> 403 (kein stiller Teilinhalt). startup_selfcheck() erzwingt Katalog-Konsistenz. Nebenlaeufigkeit (Build-325-Lehre): KEINE geteilte Connection - jeder Aufruf oeffnet kurzlebige read-only Verbindung (file:...?mode=ro). (4) NEU management/server/management_handler.py (ManagementRequestHandler, ManagementHTTPServer): duenne GET-only-Huelle; delegiert an app.dispatch; /events = SSE-Tick (sofort 'hello' mit tip_seq, dann Poll: steigt die Spitze -> 'changed', sonst 'keepalive'; RFC-8895-Rahmen via format_sse_event, eigene Maschinerie, nicht B3/4). POST/PUT/DELETE/PATCH -> 405 (read-only). ThreadingHTTPServer, daemon_threads, bindet localhost. (5) NEU tests/test_management_server.py (M01-M11): whoami; overview Gating + Scope (eigene filtert, alle=alle); integrity Gating; Shell; 404/static; SSE-Format; startup_selfcheck gruen + Luecke->RbacCatalogError; IdentityResolver Mock + unbekannt; LIVE-Server GET + POST->405; LIVE-SSE 'hello'; Read-Only-Nachweis (audit_log unveraendert). BEWUSST NICHT ENTHALTEN (Folge-Build, browser-verifizierbar): gerenderte Cockpit-Oberflaeche mit policy-getriebener Navigation/Tabs, /static-Assets (cockpit.*), Wiederverwendung dashboard.js/support_overview.js/workload.js, ECharts/Tabulator, SSE-Client. Diese brauchen Live-Browser-Abnahme (hier nicht moeglich) -> console-first, sobald verifizierbar. REGRESSION run_tests.py: Python 855 passed (844+11) / 59 skipped / 3 subtests, JavaScript 467 passed / 1 skipped / 1 todo (kein JS geaendert). DEPLOY: keine Migration; reiner Lesepfad. Start: python management.py --coordinator-db PATH [--auto-port --open-browser]; Identitaet aus OS-Konto (oder --as-user). HINWEIS: Bauplan-Detaildatei im ZIP, per .gitignore (*.md) nicht getrackt -> 'git add -f'. GELIEFERT: siehe ZIP (per-file MD5)."
}

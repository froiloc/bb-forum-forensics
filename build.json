{
  "build": 412,
  "version": "0.7.412",
  "date": "2026-07-14",
  "note": "Build 412 (Baustelle 6/7, Vermaehlung Berichtseditor B6 x Management B7, Welle 1) - SF-3 BACKEND: KOMMENTAR-BRUECKE ueber ADDENDUM-DATEIEN. Autoritativ: mc 2026-07-14 (Konzept v0.2 §4). Basis Build 411 (45cefe0); MD5-Handshake bestanden. SCHNITT (bewusst, mc-Regel 'jede Version lauffaehig+getestet' + JS-Protokoll): B-c aus Konzept §6 buendelte SF-3 (Backend) + W4 (Frontend). Dieser Build liefert NUR das SF-3-BACKEND (Schreib-/Lesepfad, voll getestet); die W4-Cockpit-Sicht folgt als eigener Build nach dem JS-Vorgehen (Console-Output -> PoC -> Rollout). MODELL (mc): 'Eine Person, ein Fall, genau EINE Datei' - jede kommentierende Person schreibt AUSSCHLIESSLICH ihre eigene Addendum-Datei, alle lesen ('lesen am Ort'; kein Transport/Merge/Loeschen). KEIN Schreiben in evidence_<uid>.db (Migrationsvorbehalt; 'nie zwei Schreiber pro Datei'). NEU: db/review_addendum_db.py - ReviewAddendumDb (+ Helfer bucket_for/addendum_path/open_addendum). Ablage ./data/evidence/addenda/<bucket>/<uid>/evidence_<uid>_<pid>.db mit bucket=md5(str(uid))[:2] (256 Buckets; haelt ./data/evidence/ ueber den 2-Jahres-Horizont schlank UND ausser Reichweite des noch loeschenden Cross-Annotation-Integrators). Schema: meta + review_comments (comment_id UUID, report_id, block_id, reviewer_pid, reviewer_role, comment_text, suggested_content, status CHECK(pending|addressed|dismissed|revoked), block_sha256, created_at, resolved_at) - formgleich zu evidence report_comments, plus block_sha256 (Blockstand zum Kommentarzeitpunkt -> spaetere Aenderung erkennbar). journal_mode=delete, BEGIN IMMEDIATE, busy_timeout (Build 408/409). NEU: management/reports/review_comment_reader.py - ReviewCommentReader vereinigt die review_comments ALLER Prueferinnen eines Falls read-only (Glob evidence_<uid>_*.db im Bucket). GEAENDERT: management/audit/event_types.py - REVIEW_COMMENT_ADDED, REVIEW_COMMENT_RESOLVED (+ ALL). GEAENDERT: management/server/management_app.py - GET /api/report/comments (Union, read-only); POST /api/report/comment (Kommentar in EIGENER Datei anlegen; reviewer_role = supervisor falls approve, sonst lector; block_sha256 read-only aus evidence); POST /api/report/comment/resolve (EIGENEN Kommentar-Status setzen; owner-only strukturell ueber den Pfad pid==person_id; create=False -> 404 falls keine eigene Datei/kein Kommentar). RECHTE: reports.review ODER reports.approve; Scope 'eigene' -> nur eigene zugewiesene Faelle. POST-Haertung unveraendert (X-AIW-Token/Origin/JSON im management_handler). PRUEFSPUR: comment-add/-resolve werden FLACH im coordinator.db-audit_log belegt (audited_write; nur Fakten user_id/report_id/block_id/comment_id/role/status, KEIN Kommentartext), BEST-EFFORT (gesperrte Kette verweigert den Vorgang nicht; Warnung statt stillem Schlucken) - identisch zur SF-2-Abwaegung (mc bitte bestaetigen). Auch das SF-2-Lese-Audit nutzt jetzt denselben Helfer _audit_best_effort (Verhalten unveraendert). MIGRATION/01.07.2026: KEINE Schemaaenderung an evidence_/forensic_/assets_/templates_; NEUE Addendum-Dateien sind additiv; coordinator.db-Zuwachs additiv (audit_log). TESTS: +8 pytest (tests/test_review_comments.py, RC01-RC08 gegen echte evidence-Fixture + echte Addendum-Dateien): RC01 anlegen (200, gebucketeter Pfad, block_sha256 korrekt); RC02 Union-Read; RC03 Union ueber zwei Prueferinnen (Rollen lector+supervisor); RC04 ohne Cap->403; RC05 eigenen Kommentar aufloesen; RC06 READ-ONLY-Integritaet evidence (MD5 vor==nach); RC07 Audit (added+resolved); RC08 resolve unbekannt->404. REGRESSION (Cloud, Python 3.11): pytest 1212 passed / 49 skipped / 6 subtests (inkl. der 8 neuen); tests/test_editor_renderer.py bleibt das bekannte Py-3.11-Artefakt (VM-3.14 gruen), fuer den Cloud-Lauf ausgeklammert. VM (Python 3.14): 'python run_tests.py' erwartet gruen. py_compile OK. NAECHSTER SCHRITT: W4-Cockpit-Sicht (Lektorat) - Frontend, nach JS-Protokoll; setzt SF-1/SF-2/SF-3 zusammen."
}
